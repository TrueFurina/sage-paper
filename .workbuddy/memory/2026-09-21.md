# 2026-09-21 工作日志（SAGE 论文线）

## 完成

1. **新建 `13_Section2_Literature_Review_en.md`** —— Section 2 文献综述英文初稿（5 页，§2.1–2.6 正文 + §2.7 诚实状态表）。
   - 组织轴：按「**约束施加在哪一层**」（prompt / 输出后处理·输入校验 / 学习者建模 / 训练目标）而非「Socratic」标签分类 —— 依据 `06` §0 的核心洞见。
   - 与 §1 明确分工：§1 走动机链，§2 走定位轴，不重述三个 failure modes。
   - 引用纪律：Suvernev 一律带「据其已公开摘要所述」；Udeshi 一律「as reported at AIED 2026」；EEG 认知债标注为二次引用待溯源；明确「并非无人测量答案泄露」（SafeTutors 有二值/序数条目），差异化改为「无人把它与连续可调强度做成关系曲线」。
   - **修掉一处引用歧义**：中文文献 Sun/Liu/Wang 与英文文献 Sun/Liu/Wang 撞姓同年 → 中文一律改全名拼音（Sun Lihui / Liu Junjun / Li Jingjing / Wang Yiyang / Hu Xiaohong / Xiong）。
   - 熊晓莉 WEI 师引用已带**主体差异说明**（教师 vs 学生），按 `06` §4 红线 4。

2. **新建 `14_Section8_Limitations_en.md`** —— Section 8 英文初稿（2 页，§8.1–8.7）。
   - 六条 limitation：A9 常数规定值（位置敏感 / 存在性与归因份额不敏感）、泄露靠人工编码（mi_hat 退化证据：平方相关 0.99988–0.99999、恒钉在裁剪上限 3.0）、模拟学生反证、单学科单学期、未取得文献的措辞限定、哪些结论脆弱。
   - §8 刻意**不写 λ\* 具体数字**，只指向 §6，避免把旧形式数字带进正文。

3. **★ 发现并修掉三处「λ\* 口径漂移」（真实缺陷，非格式问题）**
   - 起因：核查发现 `01` 写 λ\*=0.30 而 `08` 顶部速览表写 λ\*=0.35 —— 违反「数字单一真值」纪律。
   - 根因：A9 由「连续/最优点形式」改为「阈值/边界形式」（D6，2026-09-20）后，只有 `01` §3.2 同步了，其余未同步。
   - 两代数字对照（**论文一律取右列**）：
     | 量 | ~~旧形式~~ | **阈值形式（现行）** |
     |---|---|---|
     | λ\* | ~~0.35~~ | **0.30** |
     | earned-solve 峰值 | ~~0.5204~~ | **0.5659** |
     | 下降侧幅度 | ~~0.1394~~ | **0.3008** |
     | 可归因份额 | ~~85%~~ | **86%** |
   - 三处残留处置（按「永不删除、只标注」铁律）：
     - `08` §一 结论速览表 → 顶部加醒目口径警告框 + 两行加删除线
     - `08` §四.4「论文可用的三句话」 → 整体标为「旧形式·已作废，仅为审计轨迹」
     - `09` §七 A9 敏感性两张表（delivery_damping 扫描） → 加口径标记；**定性结论成立**（λ\* 随 D 从 0.70 移到 0.25），**数字须随 D9 重跑**
   - 已登记进 `12` 待决策清单：挂 **D9 连带发现**（M2 重跑时一并清理）+ C 类已完成项。

4. **同步更新 `11_论文成稿进度地图.md`**：成稿率 20–25% → **40–45%**（约 15 页 / 目标 20–50 页）；初稿节数 2 → **4**（§1/§2/§3/§8）；空白 8 → **7**；文件角色表加 `13`、`14`。

## 待办（下一次接续）

- **D12 只有用户能给**（伦理审查 / 课堂 / 样本量 / 数据采集范围）→ §8.4 定稿前必须知道样本量，因为「可达成统计功效」本身就是一条 limitation。
- **D9 未定**（三级脚手架按「接近度」还是「模态」）→ 阻塞 §4、§5、`05` §4.2 那张图，且 M2 需重跑。
- 剩余空白 7 节：Abstract（1页）、§4 系统设计（6页，阻塞）、§5 方法（5页，阻塞）、§6 结果（4页，数据齐）、§7 讨论（4页）、§9 结论（1页）、附录 E 双语参考文献（未建）。
- §6 与 §7 相互依赖；建议先 §6（数据已齐）再 §7。
- 附录 E 双语参考文献：需从 `13` §2.7 + `07` 出处表汇总，9 条元数据待补（含中文文献卷期页）。

## 追加（同日凌晨：实证复跑 + §6）

5. **复跑主 sweep，实证拿到 §6 全部真值**（`python m2_lambda_sweep.py --seeds 40 --problems 25 --outdir data`，exit 0）
   - 完全复现：`θ=0.3578`、λ\*=**0.30**、`gain*=0.5659`、标定门 G1–G5 全 ok、P1–P6 全 PASS。
   - **新拿到两个绝对值**（此前只有百分比 86%）：P6 对照臂 `left_effect=+0.0594`，观测 `observed=+0.4380` → 86% 由此自洽。
   - 全曲线（40 seeds）：λ=0 → ALR 0.8524 / gain 0.1279 / leak-solve 0.6700；λ=0.3 → 0.0863 / 0.5659 / 0.0733；λ=0.8 → 0.0004 / 0.2651 / 0.0003。上升侧 **+0.4380**，下降侧 **+0.3008**。

6. **★ 发现 `data/` 主结果文件被小规模运行覆盖**（真实隐患，非格式问题）
   - `data/m2_lambda_alr_summary.json` 实际是 **16 seeds / 16 problems / θ=0.179**（gain@0.3 = 0.5049），
     而 `data/README.md` 与 `01` 描述的是 **40 seeds / 25 problems / θ=0.3578**（gain 0.5659）。两者**不是同一次运行**。
   - 处置：重跑 40 seeds 覆盖回正确配置；**旧 16-seed 文件已备份**为 `data/m2_lambda_alr_summary_SEEDS16_BACKUP_20260921.json`（+ raw.csv 同），零删除。

7. **★ 变异检验口径同步：9/9 → 13/13，并发现一条对 D12 有约束力的实证**
   - `08` §五 记录的 **9/9** 是历史版本；现行脚本 **13 项**（新增 M8 阈值 / M9 边界左移 + 3 个 liveness）。
     2026-09-21 实测 `--seeds 16 --problems 16` → **13/13**，exit 0。
   - **关键副作用**：`--seeds 8 --problems 8` 下 **M3 FAIL**（leak_durable=0.50 应 REJECTED，实测 SUPPORTED，脚本自行拒绝背书 Proposition 1）；16 seeds 起恢复正确。
     → **归因判据不是样本量无关的**：样本不足时会把泄漏通道的效应错记到脚手架账上，**恰好犯了该装置本应防止的错误**。
   - 已写入 `15` §6.1.4（作为公开结果报告），并登记为 `12` **D12 的硬约束**：M3 功效必须有明确下限，功效计算不得推迟到分析阶段。

8. **新建 `15_Section6_Results_en.md`**（Section 6 英文初稿，**半成稿**）
   - §6.1 仿真研究（有数据、有复现背书）：曲线表、边界 λ\*=0.30（区间 [0.20,0.45]）、归因 86%（76–88%）、变异 13/13、§6.1.4 样本量警告。
   - §6.2 课堂研究 **显式标 `NO DATA`** + 预先固定的分析计划（主结局 = 无泄露独立解出；泄露双编码），避免事后挑分析。
   - ⚠️ 有意设计：宁可标 `NO DATA`，也不让 §6 看起来"写完了"。

9. 同步更新：`08` §五 标题与口径说明（9/9→13/13 + 8-seed M3 FAIL 记录）；`12` D12 新增硬约束 + C 类已完成项；`11` 成稿率 → **45–50%**（约 17 页），初稿 5 节（§6 标"半"）。

## 追加判断（2026-09-21 晨，用户问"方向是不是已经定了"）

给用户的三层结论（备忘，避免下次重算）：

1. **方向已定，无需再动。** D1–D8 已裁决；`07` §7.5 做过两轮撞车检索，结论是"不需重构"。D9–D13 全是**执行口径**，不是方向。
2. **但「这条路的终点」没定** —— M3 不是一个执行细节，是**论文能否维持现有宣称等级的分水岭**：
   - 不依赖外部条件的：归因装置（第一宣称）、题名、边界语义综述定位。
   - **完全吊在 M3 上的**：贡献(3)「真实 408 课堂 + 图锚定学习者模型」、`15` §6.2 整个课堂章、
     以及「FEDC 会不会把它读成纯仿真」这件事（FEDC 已发 MAS 论文清一色真实课堂准实验 —— 见 FEDC 报告 §5 P0）。
   - 半依赖（内部，随时可定）：D9 → M2 重跑。
3. **★ 反直觉判断：小样本 M3 是负收益选项，不如不做。**
   依据 = 本轮 8-seed 教训：样本不足时归因判据会把泄漏通道的效应错记到脚手架账上，
   即**做出来的结果会系统性地偏向我们想证明的方向** —— 那比没有数据更糟（无法发、发了是错的、还会被当成注水）。
4. ~~提议：用模拟器反推样本量~~ → **已做，但改了方法**：不用模拟器效应量（循环论证，见下 11）。

## 追加（2026-09-21 晨①：M3 准实验设计 + §5）

10. **新建 `16_M3真实课堂准实验设计.md`**（M3 完整设计 + 团队实施手册，中文）
    ★ **核心设计决断**：归因必须长在**设计**里而不是**分析**里
    → 双通道（守约 / 脚手架）可独立开关 ⇒ **必须存在 B 臂「守约但无引导」**。
    归因对照的真实对应物 = **A − B**（脚手架真实贡献）、**B − C**（单纯少泄露的贡献）。
    **B 臂不可省略**；若伦理不批 B 臂，则归因在真实世界无处落地，须回到设计阶段，**不得偷偷省掉后仍宣称做了归因**。
    ⚠️ B 臂有真实伦理张力（学生拿到"会拒绝但不会教"的导师）→ 三条缓解：暴露限于单个单元 / 事后补偿 / 知情同意明示。
    主结局 = **无泄露的独立解出率**（总解出率会让泄露解与独立解一比一抵消，是实测过的坑）。

11. **★ 算出 M3 样本量（解除 D12 第 3 项的一半阻塞）** —— `_power_calc.py`，可复现
    **关键方法论点**：**不用自己模拟器的效应量算 M3 样本量 = 循环论证**（学习器模型是我们自己规定的），
    正确做法是用**外部文献**效应量。

    | d | 读法 | 每组(t) | 每组(ANCOVA, ρ=.5) |
    |---|---|---|---|
    | 0.30 | 小效应 | 175 | 132 |
    | 0.40 | 小到中 | 99 | 75 |
    | **0.50** | **Brender 2026 量级** | 63 | **48** |
    | 0.60 | 乐观 | 44 | 33 |

    → **d=0.50、四臂：每组 48 → 分析样本约 190；+20% 流失 → 招募目标约 230。**
    ⚠️ 数字**按学生计**（同一学生观测不独立），不是按「学生 × 题目」观测数。
    **谈判口径**：每组 ≥48 → 接；约 30 → 只跑「可行档」（两臂、只报 RQ1）；<25 → **不要做**。

12. **新建 `17_Section5_Method_en.md`**（Section 5 英文初稿，含 D12 方括号占位）
    §5.1 四臂 / §5.2 被试（含 5.2.1 为何不用模拟器效应量、**5.2.2 研究不该低于的下限**、5.2.3 聚类）/
    §5.3 材料 / §5.4 测量（禁 mi_hat、不用时长指标、只称"日志行为指标"不称"已验证画像"）/
    §5.5 流程（延迟测第 10 周）/ §5.6 分析预注册（`份额 = (A−B)/(A−C)`，按学生聚类重抽样）/ §5.7 伦理。

13. 同步：`11` 成稿率 → **50–55%**（约 22 页，**其中约 4 页是占位**）、初稿 6 节（§5 含占位、§6 半）、文件 18 个；
    `12` D12 补入完整功效表 + 谈判口径。

## 追加判断（2026-09-21 上午②：用户质疑"是否必须真实实验"）

用户指出 **SocraticLM（NeurIPS 2024 Spotlight）纯模拟（零真实学生）也发了顶会** → 质疑"必须真实课堂"的说法。

**我此前的"必须"说重了，已纠正。准确表述**：
> **做了真实实验才能维持现在的宣称；不做，就必须改宣称。**

**SocraticLM 能纯模拟发顶会的两个条件，我们都不具备**：
1. 它投 **NeurIPS**（ML 会议：看方法新颖性 + benchmark），我们投 **FEDC**（教育期刊：看学习增益 + 生态效度）；
2. 它把"无真实学生"写成**自己的 limitation**（被容忍但记为缺点）；Chudziak 更被审稿人直接点名
   "lack of evaluation with real students"。

**★ 真正的约束不是发表门槛，是论证自洽**（这是最有说服力的回答）：
我们 §2 正是拿"缺真实评估"当别人的缺口批评的；自己也不做 → 被一句
"你批评的缺口你自己也没补"击穿。

**四条路径（已登记为 `12` D20，且 D20 定为最上游决策，优先于 D9–D13）**：
| 选项 | 内容 | 代价 |
|---|---|---|
| ① 做真实课堂（现行） | 四臂准实验 | 伦理 + 课堂 + ~230 人 |
| ② 改为方法论论文 | 纯仿真验证装置 | **须同步收窄 §2 批评火力 + 删贡献(3)**；FEDC 仍有"无实证"风险 |
| **③ 折中：真实可行性验证** | 20–30 人单次课，**只证明装置能在真实日志上跑通** | 成本最低、保住"不是纯仿真" |
| ④ 换投 ML/AI 会议 | — | 脱离 FEDC 线 |

**★ ③ 与"样本量下限"不矛盾的关键区分（备忘，别再混淆）**：
- **效应估计**（"SAGE 有没有效"）→ 需每组 ~48 → 小样本不可靠；
- **可行性验证**（"装置在真实数据上能不能跑"）→ **可以 pilot 规模**；
  ⚠️ 但受"样本不足时判据偏向"约束 → **不得报份额点估计并据此下结论**。
- 即：小规模真实 pilot **只能说"装置可用"，不能说"因此证明了脚手架贡献 X%"**。

## 追加（2026-09-21 上午③：查证 FEDC 期刊实际实验做法）

用户问"目标期刊别的文章的实验都是怎么做的" → 联网查证期刊官网（journal.hep.com.cn/fed），取得真实元数据。

**FEDC 21(2) 2026（最新期）四篇相关论文，元数据已核**：

| 页码 | 作者 | DOI | 研究类型 |
|---|---|---|---|
| 105–125 | Huang Changqin, Zhong Yihua, Wang Xizhe, Han Zhongmei, Wei Tongquan | 10.3868/s110-021-026-0006-5 | **准实验**，英语阅读，SAS vs MAS，ARCS 动机模型 |
| 126–149 | Wang Zhaoxue, Wu Fati, Guo Zihan, Zhang Muhua | 10.3868/s110-021-026-0007-2 | **准实验四轮任务**，六智能体+三层任务-知识-思维图谱 vs 单智能体，本科生 |
| 150–174 | Ma Liping, Zheng Xiangrui, Zhou Xuehan | 10.3868/s110-021-026-0008-9 | **大样本问卷**，12,678 名本科生 / 20 所高校，潜在剖面分析+回归 |
| 175–198 | Sun Yanyan, Huang Yingfen, Wen Sifan | 10.3868/s110-021-026-0009-6 | **对话文本分析（质性，非实验）**，三种交互模式含 answer-oriented mode；**译自《现代远程教育研究》2025(3): 102–112** |

**★ 两个改变判断的发现**：

1. **FEDC 也发纯框架/设计论文** —— 20(4) 2025 有 Lu Yu, Yu Jinglei & Chen Penghe,
   "Design and Application of Pedagogical Agent with Foundation Models", 20(4): 377–397,
   DOI 10.3868/s110-020-025-0020-3，**摘要无实证评估**。
   → **「FEDC 必须真实实验」说过头了**（我此前也说过头）。准确说法：
   **实证论文的范式是准实验；非实证的方法论/框架论文同样在发。期刊要求的是宣称与证据匹配。**
   → D20 路径②（纯仿真方法论论文）的风险由「高」下调为「中」。

2. **我们的 §1.1「answer-oriented mode」出自 Sun et al.，而它是对话文本分析、不是实验**
   → 引用时**不得暗示它由对照研究确立**。已写入 `13` §2.7 与 `12` D20。

**附：同类小样本实证也能发**（非 FEDC，但可作路径③的旁证）
- Zhu et al. 2026, *Frontiers in Education*：**单组前后测 pilot，36 名中学生**，明确自陈"feasibility study rather than systematic full empirical verification"，配对 t 检验 d=1.32。
- 中文期刊例：27 名师范生单组实验；61 人（32/29）准实验。
→ 支持**路径③「真实可行性验证」**（小规模只证装置可跑，不证效应）。

**文书更新**：`13` §2.7 第 7 项改为"已核"+ 新增完整元数据表与两条引用后果；`12` D20 补入证据与风险下调。

## ★ 追加（2026-09-21 上午④：**方向重大调整** —— 转密码学 + 完全模拟）

用户指令：**① 完全模拟准实验，不做真实实证；② 方向不 408，完全转密码学（导师教研究生）；
③ 核心仍是本项目 MIR3 / WALL / 图智能体 等 4 个内容**。附导师申报材料。

**导师文件已下载并成功提取**（`_refs_passwords_course_outline.docx` → `_docx_text.txt`）。
⚠️ **工程教训**：① PowerShell 命令含 `%XX`（URL 编码）会被安全策略误判为 cmd 环境变量语法 → **改用 Python 脚本文件下载**；
② docx 内 `<w:t>` 嵌套杂标签 + PowerShell 重定向会改编码 → **正则只取 `[^<>]*`，并由 Python 直接写 UTF-8**。

**导师文件性质 = 研究生教改项目申报书（不是论文）**
题目：《基于领域RAG与多智能体协同的密码学"理论—实践"融合培养与智能督导体系研究》
架构：一底座(密码学RAG)+一中枢(MAS)+双轨(理论/实践)+一闭环(智能督导)；
四 Agent（理论诊断/代码工程/交叉应用/督导总控）；FrugalRAG；GOMARL；
内容链：数论(有限域/椭圆曲线/格)→对称/公钥/哈希/IBE→IND-CPA/IND-CCA2/归约证明→国密SM2/3/4/9→PKI/MPC/HE/ZKP→智能合约/DKG/门限签名/跨链；
五维画像：数学推导力/代码实现力/协议设计力/前沿融合力/科研逻辑力。

**★ 撞车实检结论（对我们不利）**：**密码学方向撞车密度高于 408**
- 🔴 **Mukherjee, Le & Chow (2025), "Generative AI-Enhanced ITS for Graduate Cybersecurity Programs", Future Internet 17(4):154**
  （卧龙岗大学 Institute of Cybersecurity and Cryptology）—— **研究生 + 网安/密码学 + 多智能体编排 LLM**，与申报书目标高度重叠。
- 🟡 IGI 章节（Universidad de Guadalajara，本科密码学 + LLM，8 学期纵向，LLM 作 adaptive scaffolding，**不控泄露**）。
- 🟡 Frontiers in Computer Science 2026（网安教育 AI 系统综述）；🟡 中文《计算机教育》（椭圆曲线 + GAI，问卷）。
→ **差异化仍只在老位置**：无人做归因分解、无人把泄露量做成随约束强度连续可调的曲线。

**★ 密码学场景的三个真利好**：
1. 答案更"贵"（完整证明/可编译实现），泄露更可判定；
2. 先修链更硬（有限域→椭圆曲线→ECC→签名）；
3. **泄露天然分级**（引理<一步推导<关键一步<完整证明）→ **正好支撑 D9 的"接近度阶梯"**，而 408 上要人为硬造。

**★ 三个风险**：① 第一宣称被稀释（跟着导师主线走 = 变成又一个教学系统，正面撞 Mukherjee）；
② **GOMARL 真训实际未跑**（属导师申报书框架的 RL 组件；**D21 已决定 SAGE 不复用导师框架**，方法栈为 MIR3/WALL/MRAgent/BayesG，**不含 GOMARL**；"不得宣称跑了"是 NetLearn/408 线的红线，**不适用于 SAGE**）；
③ "完全模拟"与"导师有研究生课"存在张力（不用真实数据则导师资源用不上；研究生课人数也撑不起四臂 230 人）。

**新增决策 D21–D23（比 D20 更上游）**：D21 导师要申报书/论文/两者；D22 真实数据到底用不用；D23 第一宣称是否坚持归因。
**我的建议：双轨** —— 申报书按导师框架，论文保留归因第一宣称只换场景。

## ★★ 追加（2026-09-21 中午：**场景迁移裁决已执行**）

用户澄清原话：**"还是之前方向，只不过是密码学应用场景"** —— 即**方向完全不动，只换场景**；导师文件**不管**。

**已裁决（D21–D23）**：
| # | 裁决 |
|---|---|
| D21 | **不管导师申报书框架**（那是独立产出物，不在论文范围）；**但取其学科内容链** |
| D22 | **完全模拟，不用真实数据** → 论文形态 = D20 路径②（方法论论文） |
| D23 | **第一宣称坚持「归因测量装置」**，不跟导师主线走（避免撞 Mukherjee） |

**★ "不管文件"的精确边界（重要区分，别一刀切）**：
- **弃用**：一底座/一中枢/双轨/一闭环、督导闭环、五维画像、培养模式改革 —— 那是**申报书语言**；
- **采用**：**密码学内容链**（数论→算法→协议→应用；含国密、IND-CPA/CCA2、归约证明、MPC/HE/ZKP、DKG/门限签名）
  —— **它就是重建 `03` 所缺的知识图谱骨架**，扔掉是浪费。

**本轮已执行**：
1. `09` **新增 §十三「场景迁移裁决」** —— 裁决内容、净收益/净代价、边界表、连带改动清单（8 项）。
2. **新建 `19_密码学知识图谱骨架.md` v0.1**：**46 节点 / 约 45 条硬先修边**，四层
   （L0 数学基础 11 / L1 体制掌握 13 / L2 安全性证明 7 / L3 工程前沿 15）。
   - ⚠️ 标注为**未与密码学专业人员核对**，先修边须导师审核后才可进论文。
   - ⚠️ 明确禁用 `03` 的「613 节点/609 边」伪数；**本次规模按实际可数计**。
3. `12` D21–D23 标记**已裁决** + 10 项连带清单进度表。
4. `00` 顶部加**场景迁移说明块**（D2/D3 已改，其余部分待同步）。

**★ 净收益关键一条（值得反复用）**：密码学的**答案泄露天然分级**（引理 < 一步推导 < 关键一步 < 完整证明），
且每一级都能锚定到图谱节点 → **D9 的「接近度阶梯」在密码学场景下不需要人为构造**。
机制不变（MIR3/WALL），但语义从"给不给答案"变为"给到哪一层推导"，**更精确**。

**★ 净代价**：场景新颖性下降（Mukherjee et al. 2025 已做研究生网安多智能体导师）；
但差异化仍在老位置（无人做归因分解、无人把泄露量与连续可调强度做成曲线）。

**⚠️ 下一轮最该做、也最容易漏的第 10 项**：既然走纯模拟，**§2 就不能再拿"别人的研究缺真实学生评估"当攻击点**
（否则"你自己也没补"即可击穿）。涉及 `13` §2.2 / §2.4.3 / §2.5、`07` §1.5 贡献(3)。

## ★★ 追加（2026-09-21 下午：**发现并发会话** + 接缝修复）

### 发现：本工作区有另一个会话在并行执行同一迁移

文件时间线（`_recent.log`）显示 **12:17–12:35 之间约 10 个文件被改，另有新建**，均非本会话所为：

| 时间 | 文件 |
|---|---|
| 12:17 / 12:18 / 12:20 | `17` / `15` / `14`、`02` |
| 12:22 / 12:23 / 12:24 | `06` / `README` / `11` |
| **12:25** | **新建 `20_密码学题库与测量.md`**（并归档 `04`） |
| 12:29 / 12:32 / 12:33 / 12:35 | `05` / `00` / `09`+`12` / `FEDC报告` |

**判定：不是冲突，是接力** —— `20` 明确引用了本会话 11:54 建的 `19` 的节点 ID（L0–L3），
说明那个会话看见了 `19` 并在其上继续。它还建了 `_archive/408_assets_for_studyhelp/`（含 `_HANDED_OFF.md`），
把 `03`/`04`/`16` + 两个画像 HTML 归档并**移交给 study-help-pro 项目**（符合"永不删除、仅归档"铁律）。

⚠️ **教训（跨项目通用）**：多会话共享工作区时，**动笔前先侦查文件时间线**（`Get-ChildItem | Sort LastWriteTime`），
不要假设"文件还是我上次看到的样子"。本次是靠 `13` §2.4.4 突然出现、`07` 贡献(3) 已变为 simulation 表述才察觉的。

### 本会话做的：并发改动留下的**接缝修复**（主干已完成，矛盾点未清）

扫描 `408 Classroom|real students|准实验|真实课堂|M3 ` 后，修复以下残留矛盾：

| 文件 | 问题 | 处置 |
|---|---|---|
| `01` §3.2 note | 还写「**M3 must supply** human-coded ALR」——但 M3 已取消，属实质矛盾 | 改为：该独立性**不会**由课堂提供；① 撤回"上界"宣称；② 仿真泄露量是**规定模型的性质而非独立测量**；列为 limitation（`14` §8.2） |
| **`10_汇报_选题过程_微信版.md`** | **整份仍是旧方向**（408 场景 + "能进真实课堂做准实验" + 旧题名 + "剩余工作：真实 408 课堂准实验"）——**这是要发给导师的稿**，风险最高 | §四 增"场景调整"段（含撞车坦白）；§九 换新题名并说明**必须明示 simulation**；§十 改为"全模拟"并新增两条 limitation（不宣称上界；不得拿"别人缺真实学生"当批评点） |
| `07` 行 286 | 对比表 SAGE 列仍写「408 准实验」 | 改为「密码学场景仿真（装置层面）」 |
| `12` D 类已裁决表 | D1–D4 仍写「真实准实验 / 计算机 408」 | 加删除线 + 2026-09-21 更新标注 |
| `FEDC报告` §5 | 整节仍是"拟准实验设计"，与同文件行 230/240 已改的"全模拟"**自相矛盾** | §5 标题加**作废声明**并指向 `17`/`19`/`20`；Method 条目、里程碑 M3 行、"下一步"全部改为全模拟口径 |

### 本会话同时完成的原计划项

- ① 收窄 §2 火力 ✅：`13` §2.4.1 删掉「measured in a classroom (M3)」这一**虚假宣称**；§2.7 新增**两条红线**
  （①不得暗示做过课堂研究——`15` §6.2 的 `NO DATA` 占位本身就是一种断言；②不得以"有无真实学生数据"评判他人工作质量，
  批评必须落在**设计缺陷**上——"没有把脚手架与少泄题分开"）。
- ② 换题名 ✅：`07` 顶部旧题名标作废 + 给出 3 个候选（推荐 A，明示 simulation），**待用户拍板**。
- ③ 题库重做 → **由并发会话完成**（`20`），不重复劳动。
- ④ §5 改模拟设计 → **由并发会话完成**（`17`）。

## ★ 追加（2026-09-21 下午②：题名定稿 + §4 完成）

**用户批准推荐题名**（原话"推荐的建议可以"）：
> *When Does Guidance Still Help? Separating Scaffolding Effects from Answer-Leakage Effects in Graduate Cryptography Instruction — A Simulation Study*

**★ 定稿过程中发现题名有 3 个不一致版本**：
- `05` / `README`（并发会话改的）："…— Evidence from a Graduate Cryptography Course" ← **未明示 simulation，会被读成实证研究**
- `07`（我给的候选 A）✅ 正确
- `09` / `12`：旧 408 版（历史记录）

→ 已统一为候选 A（**6 处**：`05` Title 段 / `07` 开头 / `10` §九 / `README` / `12` D8 / `09` §十 D8 加作废标注）。
**纪律记牢**：题名末尾 "A Simulation Study" 是**诚实性要求**，不是措辞偏好——研究形态为全模拟，不明示即误导。

**新建 `21_Section4_System_Design_en.md`**（Section 4 英文初稿，6 页结构）：
- §4.1 五智能体 + 题库（含**归因相关的分工说明**：A3 受守约目标约束、A2/A5 属脚手架通道 → 两通道可独立开关）
- §4.2 三级脚手架（★ 采用**接近度**而非模态定义；论证密码学的接近度是**学科自带**的）
- §4.3 要答案拦截（**降级为子问题而非拒绝**；约束设在**第一次**索要处，依据"提示是有去无回的级联"）
- §4.4 知识图谱与自适应出题（**明示支撑组件、不宣称新颖性**）
- §4.5 示例对话（明确标注 **constructed, not recorded**）
- §4.6 学习者视图（次要；掌握度只作效标不进测量模型）

**同步**：`11` §4 → 英文初稿；成稿率 **55–60%**（约 27 页）；初稿 **7 节**（1/2/3/4/5/6半/8）、空白 5 节。

## 下一轮建议

- **§7 Discussion** 可写（依赖 §6，现已半成稿；但真实课堂结论仍缺，讨论须以"装置"为主轴）。
- **附录 E 双语参考文献**：9 条元数据待补（中文文献卷期页需人工查）。
- **D12 剩下三项**（伦理 / 课堂 / 采集范围）仍只有用户能给；其中**伦理能否批 B 臂**是归因存亡的关键。
- D9 未定 → M2 是否整体重跑（连带清理旧形式数字）。
- ⚠️ **工程教训（本轮实证）**：长文档写入时曾混入"乱码串字"（如"可达成功效"、"镇居而 Ke 7.3"等孤立英文/错字 token），
  `16` 已整体重写并逐句核对。**今后写入长中文文档后必须抽查关键句，不能写完就交付。**

- **§7 Discussion** 可写（依赖 §6，现已半成稿；但真实课堂结论仍缺，讨论须以"装置"为主轴）。
- **附录 E 双语参考文献**：9 条元数据待补（中文文献卷期页需人工查）。
- **D9 / D12 仍是两个硬阻塞**：D9 决定 M2 是否整体重跑（并连带清理旧形式数字），D12 决定 §6.2 与 §8.4 能否定稿。

## 2026-09-21 10:40 · 跨项目：为密码学教改项目产出教学模式图

- 定位确认**独立项目目录**（不在本 workspace、自带 .workbuddy）：
  `E:\Program\基于领域RAG与多智能体协同的密码学“理论—实践”融合培养与智能督导体系研究`
  （闽江大学研究生教改立项，2026.9—2028.8；架构口径"一底座一中枢双轮驱动一闭环"）。
- 交付：按其参考图（网安版"4.2 创新网络空间安全理论教学模式 / PRIDE"）**1:1 复刻版式、仅全面替换文字**为密码学口径 →
  `outputs/4.2_创新密码学理论实践融合培养与智能督导模式.html` + 同名 PNG。
- 沉淀复用脚本 `outputs/_html_shot.py`（Chrome/Edge headless 把 HTML 渲染为 PNG；含中文路径绕行）。
- **口径**：图内未引入任何未证实数字或合作单位——原"社会课堂"下 3 家合作企业名改为行业场景描述（关基/金融/政务），
  避免发生型证据伪造；该项目红线（证伪数字 45%/13%/500 条禁用等）未触碰。
- **工程教训**：Chrome `--headless --screenshot` 的输出路径若含中文+全角引号会静默失败 →
  先落系统 TEMP（纯 ASCII）再 `shutil.copyfile` 到目标路径。

## 2026-09-21（下午续）· SAGE 全量转密码学 —— 正文联动改写 + 408 资产移交

**背景**：上午已落定 D21–D23 裁决与 `19` 密码学图谱，但正文连带改写（12 待决策清单里的 4–10 项）尚未执行。本轮把正文逐节改到密码学 + 全模拟口径。

### 已完成的联动改写
1. **`17_Section5_Method_en.md`**（整体重写）：准实验设计 → **全模拟研究**（D20 路径②，无真实被试）；§5.2「Participants」→「Simulated learners + 规定式学习者模型」；分辨率下限从"课堂功效"改回"仿真配置 ≥16 seeds"；§5.3 材料改为**密码学题库**；§5.5 流程去掉 6 周课堂/延迟测；§5.7 伦理——无真实被试，Arm B 改为装置内效度属性而非伦理负担；诚实注记 D12 标 N/A、M3 标 Dropped。
2. **`15_Section6_Results_en.md`**（整体重写）：§6.2 原 `NO DATA` 课堂章 → **「全模拟范围说明」（M3 取消）**；§6.1.4 把"约束课堂功效"改为"约束仿真配置 + 未来课堂扩展"；§6.4 末句改为"对人是否成立的验证留给 §8.7 未来工作"；诚实注记 M3 改 Dropped、8-seed 改分辨率下限。
3. **`14_Section8_Limitations_en.md`**（4 处编辑）：§8.3「课堂是唯一学习宣称来源」→「本文无任何课堂数据，学习宣称留给未来工作」；§8.4「单域=408 课堂」→「单域=研究生密码学 + §19 图谱」；§8.7「扩展课堂研究」→「真实密码学课堂验证」；诚实注记 D12 标 Superseded。
4. **`02_系统架构与交互协议.md`**（整体重写）：标题/表头「408」→「密码学」；五智能体「408 实例」列、三级脚手架、要答案拦截、示例对话（ElGamal/RSA/归约证明）全部换成密码学实例；学习者模型 B 视图「408 四科」→「密码学 L0–L3」、C 视图 `03`→`19`、口径 `04`→`20`。
5. **`06_苏格拉底引导代表作精读与缺口分析.md`**（5 处编辑）：G4 两处「M3 真实 408 课堂」→ 标注 D20 路径②不做真实评估、缺口转 §8 limitation；Brender「用 408 本科生补充」→ 全模拟不再补；G8「408 四科知识图谱」→「密码学知识图谱 §19」；G13「本文场景（408 引导对话）」→「密码学」；§7 P2「04」→「20」。
6. **`00_定位与口径_术语翻译表.md`**（3 处）：§1 三句话实证句「真实 408 课堂准实验」→「研究生密码学 + 全模拟 + 装置层面归因验证」；差异化一句话去掉"首个在真实 408 验证"改为"首个做归因分离"；§3.2 产品对照加场景说明（SAGE 已转密码学，产品对照保留为 prompt 层竞品证据）。
7. **`05_论文大纲.md`**：写作纪律 guardrail「不得与真实课堂证据混谈」→「本文不做真实课堂研究，一律 simulated learners」。
8. **`11_论文成稿进度地图.md`**（整体重写）：场景 408→密码学；03→19、04→20；M3 取消、D12 N/A；成稿率口径同步。
9. **新建 `20_密码学题库与测量.md`**：替代已归档的 `04`；含三层题型（推导/实现/审查）、标注维度（知识点锚定 `19`、层、难度带 1–5、far-transfer）、跨四层示例题、Telling@N 编码手册（继承 `04` 口径 + 密码学"完整答案"分级）、抽样/自适应出题方案。**诚实标注**：是构造型评价工具、非已建成 480 题；规模目标 ≈300、逐题待建。

### 408 资产移交（解开上午的卡点）
- **卡点真相**：用户给的 `E:/MARL/study-help-pro` 在本机实际位于 **`E:/Program/MARL/study-help-pro`**（缺 `/Program` 段）；上午查 `E:/MARL` 缺失而误判不存在。
- **处置**：6 个 408 文件（03/04/16 + 两份画像 HTML + README）已 `mv` 至 `E:/Program/MARL/study-help-pro/archive/sage_408_handoff/`（未做 git 提交，由该仓库负责人决定）；SAGE 侧 `_archive/408_assets_for_studyhelp/` 留指针 `_HANDED_OFF.md`。符合「永不删除、仅移动」铁律。

### 一致性扫描结果
- 全仓 grep 408/M3/D12：正文论文文件（`00/02/05/07/13/14/15/17/11/20`）现已一致；残留 408 仅在**决策/审计历史文档**（`09`/`12`/`18`/`10`/`08`）中，作为"旧决策记录"保留属正常，且这些文档均已显式记载 D20/D21–D23 新裁决。
- `07` Introduction 经上午改写已清洁；`13` §2.4.4 用"more crowded than the 408 setting we left"作对照，符合新口径。

### 仍 open（下次接续）
- D9（脚手架阶梯按接近度/模态）未定 → 若改，M2 须整体重跑、`01`/`15`/`17` 须复查。
- 空白节：Abstract / §4 系统设计正文（阻塞，待 `02`/`19` 改写成叙述）/ §7 讨论 / §9 结论 / 附录 E 双语参考文献（9 条元数据待补）。
- `19` 图谱先修边须密码学专业人员核对后才可进论文；`20` 题库逐题待建 + 难度带标定。

### ★ 2026-09-21 13:30 纠正：SAGE 论文不涉及 GOMARL（用户指正）
- **用户原话**：「本论文不涉及 GOMARL 啊」。
- **事实纠正**：SAGE 方法栈为 **MIR3 / WALL / MRAgent / BayesG**（见 `02`/`17`），**不含 GOMARL / FrugalRAG**。GOMARL/FrugalRAG 是**导师申报书框架**里的 RL/检索组件，按 **D21 已决定不复用导师框架**（仅复用密码学内容链），故不属于本论文。
- **上轮报告误将"不得宣称跑了 GOMARL"写成 SAGE 红线** —— 那是 **NetLearn / study-help-pro（408）线**的红线，与 SAGE 无关。已清除：
  - `11` 第 104 行（误写的 GOMARL 红線 → 改为「方法栈澄清：不含 GOMARL」）；
  - `18` 第 35 行（导师框架技术栈 → 加注"仅复用内容链，不复用框架"）、风险 2（改标题"属 NetLearn/408 线，非 SAGE"）；
  - `09` 第 582 行（GOMARL 行 → 标"✅ 不采用"，红线属 408 线）；
  - `12` 第 319–321 行（"迁移硬红线" → 改"跨项目备忘，SAGE 不适用"）；
  - 本记忆文件第 207 行、第 342 行（同上纠正）。
- 全仓 grep 复核：剩余 GOMARL 引用均已显式标注"导师框架/NetLearn-408 线，非 SAGE"，论文正文（00/02/05/07/13/14/15/17/19/20）零出现。

## ★ 追加（2026-09-21 下午③：§7 讨论完成 + 三处遗留不一致修复）

### 新建 `22_Section7_Discussion_en.md`（Section 7 英文初稿，4 页结构）

以「装置」为主轴，不依赖任何结果新数据：
- **§7.1 归因结果授权了什么、没授权什么** —— ① 曲线不是发现（"降泄露+提学习"是包，Arm B 才是拆解；+0.0594 vs +0.4380 → 86%）；② **份额不是自然常数，是"泄露的答案有多教得会"的函数**（instructive-leak 变异体把它压到 43% 并触发拒绝；若真实学习者靠近那一端，86% 会显著变小，我们不知道他们在哪）；③ **可分离性是架构前提而非分析选择**（回指 §4.1 的分工设计）。
- **§7.2 边界 = 辅助不再成其为辅助之处** —— 两种相反失效（λ=0 买来的解 0.670 vs 自解 0.128；λ=0.8 未解出 0.735 且曲线**触底走平** 0.2642→0.2651）；collapse 的教育学读法（productive difficulty → impossibility，Kapur 2008）；**明确拒绝推荐 λ=0.30**（位置随规定常数在 [0.20,0.45] 移动），改用可观测的 **delivered fraction ≈0.94**（与 depth 无关，更稳定）；题名的 "when" 决定于**第一次**提示（Goldin 2012 级联 87%/88%）；末段坦白**密码学是仪器便利而非教育宣称**。
- **§7.3 与已发表工作的关系**（三条，均声明"不取代"）：SocraticPO = **奖励侧对偶**（他们管"帮助是否被记功"，我们管"帮助里含什么"；他们 Fig.5 观测到"提示过直接有害"却未量化 → 我们的 λ–ALR 曲线就是那个量化）；Mukherjee et al. (2025) = **同场景不同问题**（明说场景新颖性被削弱；IGI 章节把 LLM 当 adaptive scaffolding 却不控泄露 = 最干净的"包"例证）；Suvernev et al. (2026) = **可训练约束不是我们的**（GRPO+对抗课程是真 antecedent；差异在"二值门 vs 分级可调 + 有泄露曲线"，且他们有真实学生，我们不宣称更好的评估）。
- **§7.4 设计启示 6 条**（暴露强度为参数 / Ship 守约单臂 / 泄露用经验指标不用训练惩罚 / 约束设在第一次提示且降级不拒绝 / 监控 delivery 与离崖距离 / 报份额及其敏感性）+ 一句政策对齐（限为"选指标的理据，非效果证据"）。
- **§7.5 一段话论证 + 四个会崩的地方**（全部指向 §8，不复述）。

### 顺手修掉三处遗留不一致（皆诚实性风险）

| 文件 | 问题 | 处置 |
|---|---|---|
| `12_待决策清单.html` D8 | **仍是旧 408 题名**（md 版已改、html 渲染版没跟上） | 换成定稿题名 + 旧题名作废说明 |
| `07` 开头 | 还挂着"新题名候选（待用户选定，不得擅自定稿）" | 改为「★ 已定稿：采用候选 A」+ 落选候选改"备查勿用"，保留六处同步清单 |
| `14` §8.2 | **"in the classroom study they will be double-coded"** —— 暗示存在课堂研究，直接违反 `13` §2.7 红线 1 | 改为：仿真内泄露量是规定模型性质（非独立测量），双编码属**未来扩展**；可靠性负担真实但被推迟，故 §6 数字一律标 simulated |

### 另修 `21` §4.3 一处错误交叉引用

原写"提示是有去无回的级联 ……（§3.4）"，但 **§3.4 是学情图记忆、不含该证据** → 改为直接引 **Goldin et al. (2012)** 并给出 **87% / 88%** 数字（出处 `12` §D 类实证表），同时在 `21` 诚实注记第 7 项登记"卷期页码待补、§2 尚未引此文"。

### 同步

`11` 进度地图：§7 → 英文初稿；初稿 **8 节**（1/2/3/4/5/6半/7/8）、空白 **4 项**（Abstract、§9、附录 E、附录 A/B/C 部分）；成稿率 **60–65%**（约 31 页）；文件角色表加 `22`。

### 工程教训（本轮实证）

- **PowerShell 工具的 stdout 在本机不回传**（跑 python 打印为空，`Out-String` 也无输出）→ 校验类脚本一律 `> file` 落盘再 Read；且控制台回显中文文件名会出现 cp936 乱码（**只是显示层乱码，文件本身干净**，勿据此误判文件损坏）。
- **bash 工具本轮整体失效**（`ls`/`head` command not found，shim 的 dirname 缺失）→ 文件操作改用 Glob/Read/Edit，不要依赖 shell。

## ★★ 追加（2026-09-21 14:00：**正文成稿前的一致性审查** —— 查出并修掉 12 处实质问题）

用户要求"有必要的话认真审查"→ 对正文 9 节做了**口径级审查**（不是排版检查）。方法：对**口径关键词**做全仓 grep
（`classroom study` / `M3` / `optimum` / `the first` / `research-gap synthesis` / `self-report` / `0.5204|85%` / `§3.4|§6.4`），
命中后逐条判断是「否定式引用（安全）」还是「实质违规（必修）」。**结果：12 处实质违规，全部已修。**

### 🔴 最严重的两处（都是虚假宣称级别）

| # | 文件 | 问题 | 处置 |
|---|---|---|---|
| 1 | `01` 附「本框架与已发工作的关系」段 | **三处 "the first" 首创宣称 + 「the first to validate it in a real classroom」** —— 与 D20 路径②直接冲突，且 `07` 红线 4 明令禁止 | 整段重写：① 明示 Suvernev 已用 GRPO+对抗课程训练守约 → **可训练约束不宣称首创**；② 改为宣称三条**可测量性**性质（标量 λ / 强度−泄露率关系 / 双通道可独立切换）；③ 结尾硬钉「validated in simulation，no classroom validation is claimed」 |
| 2 | `07` §1.4 **与** §1.5(1) | 两处引用 **"AIED 2026 research-gap synthesis"** —— **该出处早已撤回**（实为 AI 生成的教育知识库），`05` §1.4 与 `10` 都已明令禁引；而它恰好出现在**支点段与贡献(1)**，是全文最关键位置 | 全部改挂 **Weidlich et al. (2025)**：Deng 元分析 19 项比较中三项非协商条件**同时满足者仅 21%** + "observed gains cannot be confidently attributed to ChatGPT" + "measure learning after the treatment is removed"（与 `13` §2.4.3 同口径） |

### 🟠 其余十处

| 文件 | 问题 | 处置 |
|---|---|---|
| `07` §1.4 | 拿「Chudziak 缺真实学生评估」当批评点 → 违反 `13` §2.7 红线 2（不得用"有无真实学生数据"评判他人，否则被"你自己也没补"击穿） | 删掉该引句，保留"性能仍依赖底层 LLM 能力"那句 |
| `07` §1.4 | "design variable with a **non-trivial optimum**" —— 与 D6 边界语义冲突 | 改为 "non-trivial structure (§2.4.1) — not, we note, with a locatable optimum" |
| `07` §1.5(3) | "a real-classroom extension is stated as future work **(§6.4)**" —— §6.4 是 Summary，未来工作在 §8.7 | 改 §8.7 |
| `07` §1.5(3) | "we **additionally report** the metacognitive calibration" —— 元认知依赖**自我报告**，仿真里不可能有；且 `21` §4.6 明写它"仅干预、不作结局" | 改为"仅作为干预项规格，本报告不将其作为结局（§4.6）" |
| `07` 红线清单 #5 / #7 | #5 仍写"给出**内部最优**"；#7 仍写"须等 M3" | #5 改为"报告边界位置区间 + 可归因份额"；#7 改为"M3 已取消 → 无任何真实数据可证明位置，只报存在性+区间+敏感性，**不得报推荐工作点**" |
| `17` §5.4 | "Two raters code independently … agreement is reported"（人工双编码）—— 与仿真-only 及我上一轮改过的 `14` §8.2 矛盾 | 改为：仿真内由模型 emission 事件判定、**无人工编码、不报信度**；双编码协议**预留给未来课堂扩展（§8.7）** |
| `17` §5.4 | secondary outcomes 含 "**self-report** measure of AI dependency" —— 仿真无自我报告 | 改为纯 behavioural（索要频率 / 是否先尝试 / 首提示后是否继续下一层＝级联） |
| `17` §5.4 末 | "would require far more participants than **a single course** provides" —— 无课堂 | 改为"需真实学习者与本研究所不具备的测量设计；本文不宣称该画像测量了日志行为之外的东西" |
| `21` §4.6 | 学习者视图被写成可展示的界面，但仿真里**没有学习者可展示** | 新增一段硬声明：**§4.6 全部未被评估**，§6 不报其任何结果；保留该节只因"视图 B 不得进测量模型"属内部效度约束 |
| `m2_lambda_sweep.py` docstring | "Real-classroom claims must come from M3" | 改为"本研究不做 M3；所有结果是装置在既定假设下的性质" |

### 新建：`23_Abstract_en.md` / `24_Section9_Conclusion_en.md`

- **`23` Abstract ≈120 词**（FEDC 100–120 上限），附**逐句映射表**（每句 → 对应章节 + 核查结论）与**红线检查清单**
  （无 students learned / optimal / first；明示 simulation + cryptography）。
  ★ **收尾句不可删**：「Results are properties of the apparatus under a stipulated learner model, not students.」——删了摘要就变成学习宣称。
  ⚠️ 待压：仍超上限约 2–5 词。
- **`24` §9 Conclusion ≈570 词**，纪律＝**不新增论断/引用/数字**，只重述。结构：形状（两侧有界）→ 方法论（归因是设计属性）→
  不宣称什么 → 两条给设计者的便宜建议 → 末段指向"真实学习者"这一未答问题。

### 进度（里程碑）

`11`：正文 **10 节**（1–9，含 §6 半）+ **Abstract 全部有英文初稿**；成稿率 **70–75%**；
**空白仅剩 2 项** = 附录 E 双语参考文献（9 条元数据待补）+ 附录 A/B/C 部分。
→ **"写章节"阶段结束，剩余是附录与参考文献。**

## ★★ 追加（2026-09-21 14:10：**附录 E 双语参考文献框架 + 联网核真 6 条**）

用户指令：**"非常重要！可以，也要一直要核查虚假宣称"** → 建 `25_AppendixE_Bilingual_References.md` 时
**先核真再落表**（附录是编造卷期页的高发区，与编造结论同罪且更易被当场查获）。

### ✅ 联网核真结果（6 条）

| # | 文献 | 核验结论 |
|---|---|---|
| A1 | **Weidlich, Gašević, Drachsler & Kirschner (2025)** | ✅ *JCAL* **41(5), e70105**，DOI **10.1111/jcal.70105**，10 页（ERIC EJ1484305 / Wiley）。→ 这是本轮改挂后的**新支点**，必须真——已真 |
| A2 | **Deng, Jiang, Yu, Lu & Liu (2025)** | ✅ *Computers & Education* **227, 105224**，DOI 10.1016/j.compedu.2024.105224（**核 A1 顺带拿到**：Weidlich 摘要里直接写了 Deng 的出处）。⚠️ DOI 年份段 2024、卷期年 2025 → 著录以 **2025** 为准 |
| A3 | **Macina et al. (2023) MathDial**（*Telling@N* 出处） | ✅ Findings of EMNLP 2023, **pp. 5602–5621**，DOI 10.18653/v1/2023.findings-emnlp.372 |
| A4 | **Mukherjee, Le & Chow (2025)** | ✅ *Future Internet* **17(4), 154**，DOI 10.3390/fi17040154，OA 19 页。**⚠️ 带回一条必须改正文的事实**：其评估是**问卷式**（内容质量／可用性／感知有效性），且自述 "**Empirical validation remains a task for future research**" |
| A5 | **Goldin, Koedinger & Aleven (2012)** | ✅ **回原文 PDF 逐字核验**（非二手）：原文确有 "ask for a second hint (**87%**) … a third (**88%**)"，Table 2 = 78/21/37/82%，"neither short nor long hint reading times are positively associated with learning" 亦属实 |
| A6 | FEDC 五篇 | ✅ 沿用 2026-09-21 上午在期刊官网核到的元数据（Huang 105–125 / Wang 126–149 / Ma 150–174 / Sun 175–198 / Lu 20(4) 377–397）；Sun et al. 的中文原版《现代远程教育研究》2025(3): 102–112 已作双语著录 |

### 🔧 核验直接导致的两处正文改写（核验结果必须回写，不能只进附录）

1. **`22` §7.3.2 与 `13` §2.4.4（Mukherjee）**：原写法会让审稿人读成"他们在真实课程里验证了效果而我们只做了仿真"——**不准确且树了一个我们打不过的靶子**。
   → 改为：明说其评估为问卷式、实证验证自述为未来工作，并明确**差异不在证据强度，而在问题**（部署与感知调查恰恰是本文没有的）。
2. **`21` §4.3 与 `22` §7.2（Goldin）**：我们原写"87% 的学习者会继续要第二层"——**比原文强一档**。
   原文是"**最可能的下一步动作**是要第二层提示（87% of the time）"。→ 两处均改为按原文措辞 + 补 Table 2 的 78%→21%。

### 附录 E 的结构（可复用）

- **§A 已核验**（可直接用）／**§B ⚠️元数据待补**（≈28 条）／**§C 中文双语 7 条**（全部缺卷期页，补齐前不得进正式表）／**§D 🚫禁引与高危**（5 条）。
- **状态标记纪律**：✅已核 / ⚠️待补 / 🔴仅摘要或二手 / 🚫禁引。**未核字段一律写 `[待核]`，禁止凭印象填数字。**
- §D 收录：**AIED 2026 research-gap synthesis（已撤回，禁引）**、EEG 认知债（二次引用待溯源）、Udeshi（会议纪要）、Suvernev（仅摘要）、一切"首创/无一"断言。

### ★ 可复用的核真方法（本轮实证，非常有效）

1. **承重引用必须回原文，不靠二手摘要。** Goldin 2012 的 87%/88% **只在全文里、不在任何摘要里**——
   若只查摘要，会得出"数字无法证实"的**错误结论**，进而误删一条真引用。
2. **抓全文的可靠路径**：`files.eric.ed.gov/fulltext/<ERIC号>.pdf`（ERIC 全文 PDF 固定命名）。
   脚本已留 `_refcheck.py`（下载 → PyMuPDF 取文本 → 正则定位数字 → 打印上下文 160 字符 → **结果落盘**，因本机会话 stdout 不回传）。
   原文 PDF 存 `_refs/ED537206.pdf` 作为证据。
3. **核一条常顺带确认相邻一条**：核 Weidlich 时顺带拿到 Deng 的完整出处（Weidlich 摘要直接引了 Deng）。
4. **核验的两类产出**：① 数字/元数据是否属实；② **措辞是否比原文强**——本轮两类都有命中，后者若放过就是变相夸大。

### 进度

附录 E 已建框架；**剩余 = 补 ≈28 条英文元数据 + 7 条中文卷期页（只能人工查）**。

## 追加（2026-09-21 夜：**正文中文要略** 全部补齐 + 长期约定入 MEMORY.md）

**用户问「正文要不要先中文」→ 我的判断：不要。** 三条理由：
① 交付物是英文（FEDC 是英文期刊），先中文后翻译 = 双倍工作量 + 一个额外的口径漂移面（本项目已吃过三次亏：题名三版本、λ\* 两代数字、§2 旧分层）；
② 卡住的从来不是语言而是论证；③ 但用户确实需要中文抓手去向导师汇报。

→ 采用方案：**每个英文正文文件顶部挂「★ 中文要略（仅供审读；非翻译源，非口径依据 —— 一切以英文正文为准）」，
250–400 字**。已挂：`23`(Abstract) / `24`(§9) / `22`(§7) / `21`(§4) / `17`(§5) / `15`(§6) / `14`(§8) / `13`(§2) / `01`(§3) / `07`(§1)。
并新建 **`.workbuddy/memory/MEMORY.md`**（项目长期约定：工作区隔离 / 语言约定 / 数字单一真值表 / 反虚假宣称红线 7 条 / 引用核验方法 / 本机环境坑）。

## 追加（2026-09-21 夜：**跨线只读核查 MARL-ECDSA**（不改其代码））

用户转来学长两条评价（① QMIX 陈旧 ② 自私智能体太理想化）+ 并发会话的评估与验收清单，要求"详细深刻评估"。
**我没有照抄转述，而是直接读了该仓库代码**（`E:\Program\MARL\【CCF】区块链AI协同：面向MARL的共识机制\marl-ecdsa-consensus-chain`）：

### 核实为真的部分（可背书）
- `marl/algorithms/mappo.py` **是真实实现**（321 行：PPO clip 0.2 / GAE λ=0.95 / ppo_epochs 4 / actor-critic / `_gae`），非桩；`train.py:167` 已接入，argparse choices 含 mappo。
- `betrayal_mode='greedy'` 已落到 `train.py:251` 与 `experiments/run_experiment.py:125`，不是死代码。
- 全仓 grep 无 `common_enemy/共同敌人/nr-54` → 与"nr-54 不实施"一致；无 hyster/nr-27 → 与"nr-27 只入日志"一致。

### 核出的四个问题（已当面指出）
1. **"1624 passed" 本机不可复现**：仓库自带 `.venv` **连 numpy 都没有** → 45 模块收集失败、仅 1100 collected；
   换系统 Python → **1526 collected / 10 errors**（缺 scipy / matplotlib / dilithium_py）。差额 98 ≈ 那 10 个模块，
   所以**不能断言造假，但也不能背书**（且我只验了 collected，未跑 pass/fail）。仓库 venv 缺 numpy 本身是可复现性缺陷。
2. **`_greedy_action` 与 docstring 不符**：docstring 说局部奖励 = -dist(自己, **分配**路标)，代码选的是**最近**路标；
   SimpleSpread 里"最近"常就是"分配" → greedy 行为与合作策略**不可区分**，"理性自私"解释力打折。
3. **"挤占他人目标点"宣称了但没实现**：`_greedy_action` 无任何"他人目标/已占据/抢占"逻辑 —— 写进 docstring 的行为必须真在代码里。
4. **`create_agents()` 工厂不支持 `betrayal_mode`**（第 170–175 行硬编码默认 'random'）→ 走工厂的实验仍是随机背叛，且**静默无告警**。
5. **口径疑点**：验收清单写"贪心伤害更大（reward −33.4 vs 随机 −53.9）"——若 reward 越大越好则 −33.4 更优，与"伤害更大"方向相反；
   须先钉死是团队 reward 还是自私者局部 reward，再入文。

### 我的独立判断（与并发会话不完全一致）
- **学长①"换算法"这个处方我不认同**：基线要经典才可比；MAPPO(2022) 也不新，且小离散动作空间合作任务上通常不优于 QMIX → 换了既没回应批评又可能让主结果变弱。
  正确处方与并发会话一致（保留 QMIX 主线 + MAPPO 作稳健性检验），但**必须把论文 claim 改成"结论跨算法成立"**——这与 SAGE 线的"重瞄"是同一招：**宣称从"新机制"换成"测量装置"**。
- **学长②比①重要，且并发会话只做了一半**：随机背叛是噪声不是策略，用它测稳健性会**低估**脆弱性（对己方结论不利的偏差，必须修）；
  greedy 是进步但不够（见上 2/3），且**若 greedy 降幅更大，绝不能写成"机制更脆弱"——那是攻击强度变了**，必须并列报两种攻击强度。
- 未跑完整测试、未跑实验、未改该仓库任何代码（仅删除自建的两个临时文件）。

### ★ 可复用的审查方法（下次直接用）

1. **先列口径关键词表**再 grep，不要逐文件通读（12 处违规靠 8 个关键词一次扫出）。
   本项目的关键词：`classroom study|real students|M3|optimum|optimal intensity|the first|research-gap synthesis|self-report|0.5204|85%|§3.4|§6.4`。
2. **命中后必做二分**：是"否定式引用"（如 "M3 is dropped" —— 安全）还是"实质违规"（如 "the first to validate in a real classroom" —— 必修）。
   只统计命中数会得出错误结论（本轮 grep 命中 ~20 条，真正违规 12 条）。
3. **重点查"支点段"与"贡献条目"**：`07` §1.4/§1.5 是全篇论证的承重墙，也是最容易被历史遗留引用污染的地方（本次两处禁引出处都在这）。
4. **交叉引用要逐条对号**：`(§x.y)` 必须真的在那一节（已抓到 §3.4、§6.4 两处错指）。

## ★★ 追加（2026-09-21 23:40：**跨线执行——服务器派单实跑**）

用户："该记录、整理、工作、在服务器上跑实验的都安排起来" → 我**实际连上服务器并把队列跑起来了**（不是只写方案）。

### 服务器现场事实（都已实测，非转述）
- `172.16.222.99`：Ubuntu 24.04 / 128 核 / **RTX 5080（Blackwell）/ 驱动 580.173.02**；本机 **SSH 密钥免密可通**（`user@`）。
- 环境：**只有 `gomarl`（torch 2.8.0+cu128）实测 `GPU COMPUTE OK`**；`mappo`(cu118)、`marl`(cu121) 在 Blackwell 上不可用（SOP 坑 2 复现）。
- **远程仓库副本是旧的**：无 `mappo.py`、`selfish_agent.py` 里 `betrayal_mode` 0 处、且不是 git 仓库 → 已同步源码（旧源码备份到 `_backup_src_0921/`，零删除）。
- 冒烟：MAPPO EXIT=0、exp_b EXIT=0、`--save` 自定义路径生效；**单任务 200 回合 ≈ 10.5 s**（→3000 回合约 2.5 min）。
- 队列：tmux 会话 `sage_dispatch` 托管（**不用 nohup**，systemd 会清进程），剧本 `_dispatch_queue.sh`：
  **顺序执行**（结果文件写死 CWD，并行会覆盖）+ **SKIP 已存在产物**（可断点续跑）+ **按 tag 归档** + `STATUS.tsv` 记 exit/耗时。
- 规模：**E13 = 自私攻击强度（random vs greedy × {0,20,50}% × 10 seed，500 回合）**；
  **E14 = iql/vdn/qmix/mappo × 3000 回合 × 10 seed**；预计总耗时约 3 h。已在跑（首个任务 99 s 完成、exit=0）。
- 记录文档：`deliverables/派单_E13_E14_服务器_20260921.md`（含监控命令、取回命令、回滚、风险）。

### ★ 三条必须记住的发现
1. **口径矛盾（最高优先）**：冒烟数据 `selfish_50pct_greedy` avg_reward **−40.4 优于** random **−66.4**，而合作率方向相反（贪心更低）
   → "理性自私是否伤害更大"**两个指标指向相反**，主结局是哪个指标必须钉死，否则答辩时这两个数会互相打架。
2. 🔴 **凭证明文泄露**：`deliverables/_d2d3/pull_new_results.py` 第 10–12 行含**明文账号密码**；该目录若进提交/分享包 = 交出服务器凭据。本次派单全程走密钥，未用该密码。
3. **greedy 语义与 docstring 不符**（选"最近路标"vs 文档说"分配路标"；且"挤占他人目标点"代码里没实现）→ 论文须按**实际实现**描述，且 nr-27 自适应版本未实施应写 limitation。
