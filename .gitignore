# ============================================================
# SAGE 论文工作区 —— 版本控制忽略规则
# 原则：入库「不可再生的成果」与「可追溯的证据」；
#       忽略「可再生的渲染件」与「外部来源的大体积文献」。
# ============================================================

# --- 渲染产物：可由 md2pdf.py 从 .md 重新生成（34 MB）---
pdf/

# --- 外部文献 PDF：体积大（181 MB）、可再次获取 ---
#     核验工具留档在 _refcheck.py；引用元数据在 25_AppendixE
refs/
_refs/
核心论文（参考）/

# --- 二进制文献与外部资料 ---
*.pdf
*.docx
*.ttf

# --- 白名单：虽命中上面规则，但入库价值高于体积成本 ---
# `_sage_subset.ttf` 是 md2pdf.py 的**运行依赖**（非纯可再生品）：
#   虽可由 _fontsub.py 重建，但缺它则渲染管线直接不可用，故入库以保证克隆即可渲染。
! _sage_subset.ttf
# 导师课程大纲原件：**已于 2026-09-22 移出版本控制**（仓库转公开前）
#   原因：它是**第三方未发表材料**，未经导师同意不应随公开仓库发布。
#   文件仍完整保留在本地工作区（仅 git rm --cached，未删除）。
#   ⚠️ 该文件仍存在于**历史提交**中（基线 d47dbb2 起）；如需彻底清除须重写历史。
#!_refs_passwords_course_outline.docx

# --- Python ---
__pycache__/
*.pyc
.venv/
venv/

# --- 编辑器/系统 ---
.vscode/
.idea/
Thumbs.db
desktop.ini
~$*
*.tmp

# --- 临时日志（每次运行覆盖，无长期价值）---
_dl.log
_recent.log
_docx_run.log

# --- 本地检查产物（含 token 前缀等，禁止入库）---
_gh_token_check.*
_gh_create_repo.py
_gh_create_out.txt
_scan_out.txt
_wc_out.txt
_docx_text.txt
_sync_src.tgz
