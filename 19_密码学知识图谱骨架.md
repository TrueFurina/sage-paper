# 19 · 密码学知识图谱骨架 v0.1（2026-09-21）

> 用途：替代 `03_408知识图谱骨架.md`，作为 **M2 仿真的学科输入**、卡点定位与自适应出题的图结构来源。
> 来源：导师申报材料的**内容链**（`_docx_text.txt`，模块二的理论/实践双轨）+ 密码学标准课程体系。
> ⚠️ **状态：v0.1 骨架，未与密码学专业人员核对。** 先修边的方向与必要性须由导师（或密码学教师）审核后才可用于论文。
> ⚠️ **规模口径**：本骨架的节点/边数按**实际建成的数量**计（下方逐条可数）。
> **禁止**再使用 `03` 中「613 节点 / 609 边」一类数字——那批数字已确认为伪数，真实规模是 86/82，本次重建从零开始。

---

## 1. 设计原则

1. **先修边必须是真的**：每条边都是"不掌握 A 就一定学不动 B"的硬依赖，不是"相关"。
   密码学的优势正在于此——它的先修关系比多数计算机科目刚性。
2. **层级对应培养阶段**：L0 数学基础 → L1 体制掌握 → L2 安全性证明 → L3 工程与前沿，与导师材料的"数论推导 → 核心算法编码 → 前沿实战"链条一致。
3. **每层都可出题**：节点须能挂载"推导题 / 实现题 / 审查题"，否则该节点对自适应出题无用。

---

## 2. 节点（四层）

### L0 数学基础（推导链的地基）

| ID | 节点 | 说明 |
|---|---|---|
| L0-01 | 群、环、域 | 代数结构基本定义与性质 |
| L0-02 | 模运算与同余 | 模加乘、逆元、中国剩余定理 |
| L0-03 | 欧拉定理与费马小定理 | RSA 与离散对数的前置 |
| L0-04 | 有限域 GF(p) | 素域上的运算 |
| L0-05 | 有限域 GF(2^n) | 扩域与多项式表示（AES 的代数基础） |
| L0-06 | 离散对数问题 | DLP，DH/ElGamal 的困难性假设 |
| L0-07 | 椭圆曲线与群律 | 曲线定义、点加、倍点、无穷远点 |
| L0-08 | 椭圆曲线离散对数问题 | ECDLP |
| L0-09 | 双线性对 | 配对运算；IBE / SM9 / BLS 的基础 |
| L0-10 | 格与格基约化 | 格定义、SVP/CVP、LLL |
| L0-11 | 学习带误差问题（LWE） | 格密码的困难性基础 |

### L1 经典与现代密码体制（理论轨道主体）

| ID | 节点 | 说明 |
|---|---|---|
| L1-01 | 分组密码与 DES | Feistel 结构 |
| L1-02 | AES 与 Rijndael | S 盒、列混淆（依赖 L0-05） |
| L1-03 | 分组密码工作模式 | ECB/CBC/CTR/GCM |
| L1-04 | 流密码 | 密钥流生成 |
| L1-05 | RSA | 依赖 L0-03 |
| L1-06 | Diffie-Hellman 密钥交换 | 依赖 L0-06 |
| L1-07 | ElGamal 加密 | 依赖 L0-06 |
| L1-08 | 椭圆曲线密码（ECC） | 依赖 L0-07、L0-08 |
| L1-09 | 密码学哈希函数 | 单向性、抗碰撞 |
| L1-10 | 消息认证码与 HMAC | 依赖 L1-09 |
| L1-11 | 数字签名（DSA/ECDSA） | 依赖 L1-08、L1-09 |
| L1-12 | 身份基加密（IBE） | 依赖 L0-09 |
| L1-13 | 密钥管理与会话密钥 | 工程侧基础 |

### L2 安全性定义与证明（研究生层次的核心区分点）

| ID | 节点 | 说明 |
|---|---|---|
| L2-01 | 计算困难性假设 | 与归约的连接点 |
| L2-02 | 攻击模型：IND-CPA | 选择明文攻击下的不可区分性 |
| L2-03 | 攻击模型：IND-CCA2 | 自适应选择密文攻击 |
| L2-04 | 随机预言机模型 | ROM 及其争议 |
| L2-05 | 归约证明（Security Reduction） | 把攻击归约到困难问题 |
| L2-06 | 可证明安全 | 依赖 L2-05，是形式化训练的总出口 |
| L2-07 | 归约证明的典型错误 | 时间界越界、预言机模拟失败等（**审查题来源**） |

### L3 工程实现与前沿应用（实践轨道）

| ID | 节点 | 说明 |
|---|---|---|
| L3-01 | 国密 SM4 | 分组密码 |
| L3-02 | 国密 SM3 | 哈希 |
| L3-03 | 国密 SM2 | 椭圆曲线公钥密码 |
| L3-04 | 国密 SM9 | 标识密码（依赖 L0-09） |
| L3-05 | 侧信道攻击与防护 | 抗侧信道评估 |
| L3-06 | 公钥基础设施（PKI） | 证书链、信任模型 |
| L3-07 | 秘密共享与门限方案 | Shamir 等 |
| L3-08 | 安全多方计算（MPC） | 依赖 L3-07 |
| L3-09 | 同态加密（HE） | 部分/全同态 |
| L3-10 | 零知识证明（ZKP） | 交互式与非交互式 |
| L3-11 | 分布式密钥生成（DKG） | 依赖 L3-07 |
| L3-12 | 门限签名 | 依赖 L1-11、L3-07 |
| L3-13 | 智能合约安全 | 依赖 L1-09、L1-11 |
| L3-14 | 跨链交互与隐私保护 | 依赖 L3-11、L3-12 |
| L3-15 | 隐私计算系统集成 | MPC + HE 的组合部署 |

**节点合计：L0 = 11，L1 = 13，L2 = 7，L3 = 15 → 共 46 个。**

---

## 3. 先修边（每条都是硬依赖）

### 3.1 L0 内部
```
群环域 → 模运算与同余 → 欧拉定理/费马小定理
群环域 → 有限域 GF(p) → 有限域 GF(2^n)
有限域 GF(p) → 离散对数问题
有限域 GF(p) → 椭圆曲线与群律 → ECDLP
椭圆曲线与群律 → 双线性对
格与格基约化 → LWE
```

### 3.2 L0 → L1
```
有限域 GF(2^n) → AES
欧拉定理 → RSA
离散对数 → Diffie-Hellman
离散对数 → ElGamal
ECDLP → ECC
双线性对 → IBE
密码学哈希函数（见 L1-09）→ 消息认证码 → 数字签名
分组密码与 DES → AES
分组密码 → 工作模式
```

### 3.3 L1 → L2
```
困难性假设 ← ECDLP / 离散对数 / RSA 问题（作为实例）
RSA → 攻击模型 IND-CPA
ElGamal → 攻击模型 IND-CCA2
哈希函数 → 随机预言机模型
困难性假设 → 归约证明 → 可证明安全
归约证明 → 归约证明的典型错误
```

### 3.4 L1/L2 → L3
```
AES → 国密 SM4
哈希函数 → 国密 SM3
ECC → 国密 SM2
双线性对 + IBE → 国密 SM9
ECC → 侧信道攻击与防护
数字签名 → PKI
秘密共享 → MPC、DKG、门限签名
数字签名 + 秘密共享 → 门限签名
MPC + HE → 隐私计算系统集成
DKG + 门限签名 → 跨链交互与隐私保护
哈希函数 + 数字签名 → 智能合约安全
可证明安全 → 零知识证明（构造与分析）
```

**先修边合计约 45 条**（上列可逐条清点；最终数以审核后定稿为准）。

---

## 4. 这张图在 SAGE 里的三处用途

| 用途 | 对应组件 | 说明 |
|---|---|---|
| **卡点定位** | MRAgent（学情图记忆） | 学生卡在 L1-08（ECC）时，沿先修边回溯定位到 L0-07（椭圆曲线群律）或 L0-04（有限域） |
| **决定下一步** | BayesG（自我图推断） | 在卡点节点的 1–2 跳局部子图上采样"最该补的下一个节点" |
| **出题** | 自适应出题 | 每个节点挂载推导题 / 实现题 / 审查题，按难度带与先修状态选题 |

**★ 这张图与 D9 的直接关系**：密码学的答案泄露天然分级（引理 < 一步推导 < 关键一步 < 完整证明），
而**每一级都能锚定到图上的节点**——"学生卡在 L2-05 归约证明的第 3 步"比 408 场景下的模糊状态精确得多。
**这就是 D9 在密码学场景下不再需要人为构造"接近度"的原因。**

---

## 5. 待审核与待补（不得跳过）

| # | 事项 | 为什么必须做 |
|---|---|---|
| 1 | **先修边方向与必要性须经密码学专业人员核对** | 我按标准课程体系构建，但"某条边是否真的硬依赖"须领域专家确认；**未经核对不得写进论文** |
| 2 | 节点是否需要细分（如把 L0-07 拆为点加/倍点/标量乘） | 取决于出题粒度 |
| 3 | 每节点的**难度带**划分 | 自适应出题需要，`04` 的难度 1–5 需重新标定 |
| 4 | 题库需重建：408 真题 → 密码学推导题 + 实现题 + 审查题 | `04` §1.3 的 480 题目标须整体重做 |
| 5 | L2 层是本方向与"通用 LLM 辅导"的**最大区分点** | 归约证明的错误类型（L2-07）是通用模型最容易出错、也最需要 RAG 锚定的地方——**值得单独做一次撞车排查** |

---

## 6. 一句话

**46 节点 / 约 45 条硬先修边，四层对应"数论推导 → 体制掌握 → 形式化证明 → 工程前沿"。**
这张图让 MIR3/WALL 的作用从"控制给不给答案"变成"控制给到哪一层推导"——**机制不变，语义更精确**。
