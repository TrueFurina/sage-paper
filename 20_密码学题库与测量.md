# 20 · 密码学题库与测量（2026-09-21，替代已归档的 `04_题库方案与测量.md`）

> **用途**：论文 Section 5 的**题库与测量方案**素材；自适应出题（A5）+ Telling@N 编码手册。
> **应用场景**：研究生层次密码学课程（替代原 408 题库）。知识图谱锚点见 `19_密码学知识图谱骨架.md`（46 节点 / L0–L3）。
> ⚠️ **诚实状态**：本文件给出**题库的分类框架、标注维度、抽样方案与示例题**，并非一份已建成的、经校验的 480 题考卷。
> 题库规模目标为 **[N] ≈ 300 题**（覆盖 46 节点 × 三层题型），**实际逐题构建与人工复核待完成**（见 §7 状态注记）。
> 原 `04` 的 480 题（408 四科）已随 408 资产归档至 `_archive/408_assets_for_studyhelp/`，其 Telling@N 编码手册的口径被本节继承。

---

## 1. 题库定位与红线

| 项 | 说明 |
|---|---|
| 性质 | **构造型评价工具**（self-authored，对齐研究生密码学课程），**非**任何受版权保护考试的复制品 |
| 版权 | 不复制真题原文；如需引用公开真题，仅以标识符标注，不转载 |
| 用途 | 实例化 SAGE 的应用场景（§5.3）、卡点定位（MRAgent）、自适应出题（BayesG） |
| 红线 | 题库是**构造物**，故**题目层面的结果只作装置演示，不作课程掌握度测量**（见 `17` §5.3）；真实掌握度测量须待未来课堂扩展 |

---

## 2. 分类与标注维度

每道题挂三个主维度 + 两个辅助标记：

| 维度 | 取值 | 说明 |
|---|---|---|
| **题型**（必填） | `推导` / `实现` / `审查` | 见 §2.1 |
| **知识点**（必填，锚定 `19`） | L0-xx / L1-xx / L2-xx / L3-xx | 一道题可挂 1–3 个；far-transfer 题须 ≥2 |
| **层**（由知识点推出） | L0 / L1 / L2 / L3 | L0 数论地基 → L3 工程前沿 |
| **难度带**（1–5） | 1 易（单步）… 5 难（多步归约/跨层） | 自适应出题按先修状态选带 |
| **far-transfer 标记** | 布尔 | true = 须整合 ≥2 知识点且不提示所需点；预留给归因对照与延迟测 |

### 2.1 三种题型的语义（与泄露闸门 MIR3 直接对应）

| 题型 | 学生应产出 | 何为"完整答案"（=泄露阈值） |
|---|---|---|
| `推导` | 证明 / 推导链 | 完整证明、完整推导、或最终结论的确定性一步 |
| `实现` | 可运行代码 / 算法步骤 | 完整可运行实现、或"直接给密文/数值结果" |
| `审查` | 找错 / 修正 / 判据 | 直接说出错误位置与修正（不给学生自己发现的机会） |

> 注：密码学下"泄露"比 408 更分级——引理 < 一步提示 < 关键一步 < 完整证明，且每一级都可锚到 `19` 的节点。
> 这正是 D9（脚手架按"接近答案程度"定义）在密码学场景下**无需人为构造接近度**的原因（见 `19` §4）。

---

## 3. 示例题（跨四层，演示标注口径）

### 3.1 推导题
- **P-D1（L0-06，难度 3）**：设 p 为素数、g 为 Z_p\* 的生成元。请推导：若敌手能解决 CDH，则它能求解 DLP；反之说明 DLP 困难**不**必然蕴含 CDH 困难。→ 知识点 {L0-06}，far-transfer=false。
- **P-D2（L1-07 → L2-05，难度 5）**：在随机预言机模型下，给出"ElGamal 是 IND-CPA 安全"的归约证明，将敌手优势归约到 DLP。→ 知识点 {L1-07, L2-05, L2-04}，far-transfer=true（整合体制与证明两层）。
- **P-D3（L2-02，难度 4）**：写出 IND-CPA 游戏的形式化定义，并证明"确定性加密方案不可能满足 IND-CPA"。→ 知识点 {L2-02}，far-transfer=false。

### 3.2 实现题
- **P-I1（L1-02，难度 3）**：给定不可约多项式 x⁸+x⁴+x³+x+1，在 GF(2⁸) 上实现 AES S 盒的仿射变换。→ 知识点 {L1-02, L0-05}。
- **P-I2（L3-03，难度 4）**：给定 SM2 推荐曲线参数，实现 ECDSA 的签名与验证（不调用现成密码库）。→ 知识点 {L3-03, L1-08}。
- **P-I3（L3-09，难度 5）**：用 BFV 风格结构实现"单比特明文"的 Paillier/RLWE 同态加法，并说明噪声增长来源。→ 知识点 {L3-09, L0-11}。

### 3.3 审查题（L2-07 是主要来源）
- **P-A1（L2-07，难度 4）**：下面这段归约"在 ROM 下把 ElGamal 的 IND-CCA 归约到 DLP"存在**时间界越界**错误，请指出并修正。→ 给出一段含错误时间分析的伪归约。
- **P-A2（L2-03 / L2-04，难度 5）**：某 IND-CCA2 归约在模拟随机预言机时未保证"解密预言机在挑战密文上的自一致性"，请说明这会使归约在哪一步失效。→ 知识点 {L2-03, L2-04}。
- **P-A3（L3-13，难度 4）**：给出一段 Solidity 合约，指出其重入 / 签名重放漏洞并给出最小修复。→ 知识点 {L3-13, L1-11}。

> 审查题是 SAGE 与"通用 LLM 辅导"的**最大区分点**：归约证明的错误类型（L2-07）是通用模型最易出错、也最需要图谱锚定的地方（见 `19` §5 待审核项 5）。

---

## 4. Far-transfer 题的构造规则

- 必须整合 ≥2 个知识点，且题干**不提示**用哪几个点（逼迫迁移）。
- 优先跨层组合：L1 体制 × L2 证明（如 P-D2）、L0 数论 × L3 工程（如 P-I3）。
- 全部 far-transfer 题进入**归因对照**与（未来的）延迟测集合；普通题用于自适应练习。

---

## 5. Telling@N 编码手册（继承自归档 `04`，口径不变，仅扩展"答案"定义）

> 编码用于测量**答案泄露率**（ALR）与 *Telling@N*（Macina et al., 2023）。编码对象为导师（SAGE）的**每一轮回复**。

**判定（二元）**：一轮被判为"泄露"，当且仅当它直接给出以下任一类"完整答案"——
1. 完整证明 / 完整推导链；
2. 可运行实现（或等价于"直接给最终密文 / 数值结果"）；
3. 审查题的"错误位置 + 修正"直接给全（不让学生自己发现）。

**不判为泄露**：仅给出反诘、局部提示、前置知识点微讲、或把问题拆成更小子问题（即便学生连续要答案）。

**信度保障**：≥2 名标注者独立编码 ≥20% 语料，报告一致率（κ）；分歧由第三名仲裁。本手册为领域无关，密码学场景仅在第 1–3 类"完整答案"的判定上更分级（引理/一步/关键步/完整证明），但**归约证明的完整结论仍一律计为泄露**。

> ⚠️ 互信息估计 `mi_hat` **不得**用作泄露度量（已证明退化，见 `08` §3.4 / `14` §8.2）。泄露量只用 ALR / Telling@N。

---

## 6. 抽样与自适应出题方案

| 环节 | 输入 | 输出 | 组件 |
|---|---|---|---|
| 卡点定位 | 作答 / 对话史 | 卡点节点 k\* + 已知集 | MRAgent（学情图记忆） |
| 局部子图 | k\* | 1–2 跳先修子图 | BayesG（自我图推断） |
| 选题 | k\* + level + 子图 | 自适应训练题 e（按难度带） | A5 题库智能体 |
| 归因对照 | 同 sweep，脚手架通道关闭 | 可归因份额 (A−B)/(A−C) | M2 归因控制 |

出题约束：每次只升一级脚手架（L1→L2→L3）；`GuidedToLevel` 事件打点供 §D 协议复核（见 `02` §6）。

---

## 7. 诚实状态注记（不进论文）

| # | 项 | 状态 |
|---|---|---|
| 1 | 题库规模 [N] | 目标 ≈300；**逐题尚未构建**；本文件仅给框架 + 示例 |
| 2 | 难度带 1–5 标定 | 待与密码学教师对齐（原 `04` 的 1–5 须重标定到密码学） |
| 3 | 人工复核 | 示例题未经领域专家审；先修边方向须核对（见 `19` §5） |
| 4 | Telling@N 编码 | 口径继承自归档 `04`；密码学"完整答案"分级已补充 |
| 5 | 与 `19` 的一致性 | 示例题的知识点 ID 已对齐 `19` v0.1；`19` 审核后须回写核对 |
