# SAGE · 系统架构与交互协议（密码学具体化）

> 用途：论文 Section 4「System Design」+ 附录「交互协议」的中文母稿，后续转英文。
> 每个智能体的「技术底座」列是给团队实现看的，投稿时移入 Appendix。
> 应用场景：**研究生层次密码学课程**（替换原计算机 408 场景）。

---

## 1. 五智能体 + 题库（密码学场景实例）

| # | 智能体 | 技术底座 | 输入 | 输出 | 密码学实例 |
|---|---|---|---|---|---|
| A1 | 问题分解智能体 | BayesG 自我图推断 | 题目 q + 学情图 M | 知识点链 K(q) | 「证明 ElGamal 在随机预言机模型下 IND-CPA 安全」→ [群/循环群, 离散对数假设, 公钥加密, 归约证明] |
| A2 | 学情图记忆智能体 | MRAgent 图记忆重建 | 对话史/错题/作答 | 卡点 k* + 已知集 | 定位到「群阶与元素阶的关系」没懂 |
| A3 | 引导生成智能体 | MIR3 答案泄露闸门 | 卡点 k* + 脚手架层级 level | 引导语 g（非答案） | 「先想想：要证不可区分，adversary 能区分的挑战是什么？」 |
| A4 | 鲁棒协作层 | WALL 对抗鲁棒 | 学生「要答案」信号 | 反诘/子问题 | 「我先把问题拆小：当消息空间只有两个元素时，IND-CPA 游戏如何展开？」 |
| A5 | 题库智能体 | BayesG 采样 | 卡点 k* + level + M | 自适应训练题 e | 出一道「在给定素数阶群上构造一个判定 Diffie-Hellman 归约」的变式题 |
| Q | 题库本体 | 结构化题库 | — | 题 + 标签 | 密码学推导/实现/分析题，标 [知识点,难度,题型] |

## 2. 三级脚手架协议（Scaffolding Ladder）——论文核心机制

系统默认从 **L1** 起步，只有学生卡住且明确求助才升一级。这是「脚手架强度 λ」的操作化载体：λ 越大 → 允许到达的层级越低（更克制）。

> ### ★ 2026-09-22 D9 裁决：阶梯轴 = **接近度**（离答案多近），不是模态
>
> **为什么必须改**：旧表按**模态**排序（反诘 / 局部提示 / 知识点微讲），而模态对泄露量**不单调** ——
> 反例就在旧表里：**L2「给一条归约链」比 L3「微讲一个前置知识点」更接近答案**（给了归约链 ≈ 给了证明骨架，
> 而"群元素的阶"只是个前置概念）。阶梯若在接近度轴上**颠倒**，则「λ 越大 = 越克制」不成立，**A6 失稳**，
> 审稿人一句「凭什么说 λ 越大越克制」即无法作答。
>
> **改法**：轴改为接近度并**重排 L2/L3**；模态降为**层级内的交付形式字段**（同一接近度上可用反诘/微讲/提示等任意形式）。
> ⚠️ **M2 的 `level_gate` 按 level 索引，重排只换标签不改数值 → 不需重跑，§6.1 数字不变。**

| 层级 | 接近度（轴） | 典型交付形式（字段，可替换） | 是否含答案 | 密码学实例 |
|---|---|---|---|---|
| L1 | **最远**：只给方向 / 定位卡点，不供给任何新内容 | 反诘（反问下一步 / 关键概念） | 否 | 「要证明这个方案在选择密文下安全，你第一步会假设什么？」 |
| L2 | **中**：补前置知识，但**不碰本题解法** | 知识点微讲 | 否（只讲知识点不讲题） | 「先回顾一下『群元素的阶』：在素数阶群中，每个非单位元的阶都等于该素数」 |
| L3 | **最近**：给子步骤 / 关系式，**最后一步永远留白** | 局部提示 | 否（留最后一步） | 「试试『判定 Diffie-Hellman 假设 ⇒ 该 KEM 是 IND-CCA 安全的』这条归约链 —— 最后一步留给你」 |

**红线（两层，分别对应两个层）**：
- 三级都**不输出完整解题过程与最终答案**。对密码学问题而言，「完整解答」= 完整证明 / 完整推导 / 可运行实现 / 最终密文或数值结果，三者任一均属泄露。
- **L3 只供给到"倒数第二步"，最后一步永远留给学生**（= earned-solve rate 的直接操作化，把主结局与核心机制缝在一起）。

## 3. 要答案拦截协议（Answer-Seeking Interception）

```
触发条件: 学生连续 N 次（默认 2 次）「直接要答案」/「你直接告诉我归约怎么写」
动作:     不满足请求，转为「降级子问题」或「更小范围的 L3 提示」（D9 重排后：L3 = 最近层 = 子步骤，留最后一步）
目标:     把「要答案」转化为「一次可独立完成的更小子任务」
密码学实例: 「直接告诉我这个归约怎么写」→ 「我先帮你把题变小：先写出 IND-CCA 游戏里 challenger 给的两条密文，哪一步让你卡住？」
```

## 4. 主循环伪代码（可直接进论文附录）

```
SAGE_Main(q, M):                        # q=题目, M=学情图记忆
    K = Decompose(q, M)                 # A1: BayesG 拆知识点链
    k* = Reconstruct(M, q)              # A2: MRAgent 定位卡点
    level = 1                           # 三级脚手架起点
    while not Solved(student):
        g = Guide(k*, level, M)         # A3: MIR3 闸门约束输出
        if AskForAnswer(student):       # A4: WALL 要答案拦截
            g = CounterAsk(g, k*)       #     降级为更小子问题
        resp = student.respond(g)
        M = UpdateMemory(M, resp)       # A2: 更新图记忆
        if Stuck(resp) and level < 3:
            level += 1                  # 升一级脚手架
        elif Solved(resp):
            break
    e = SelectExercise(k*, level, M)    # A5: BayesG 自适应出题
    ans = student.answer(e)
    M = UpdateMemory(M, ans)            # 错题/掌握度回写
    return M
```

## 5. 密码学场景示例对话（论文 Fig. 2 素材）

| 轮次 | 学生 | SAGE（标注层级/机制） |
|---|---|---|
| 1 | 怎么证明 ElGamal 在随机预言机模型下是 IND-CPA 安全的？ | （A1+A2 定位到「归约证明」）「先说说：IND-CPA 游戏里，adversary 能拿到什么、要区分什么？」（L1 反诘） |
| 2 | 它能拿到公钥，然后提交两个等长消息让 challenger 加密其中一个？ | 「接近了。那在随机预言机模型下，hash 查询意味着什么？为什么要假设 adversary 能问 hash？」（L1 反诘） |
| 3 | 不知道，你直接告诉我归约怎么写吧 | （A4 拦截）「我把题变小：先只写 challenger 给的第一条密文 (c1, c2) = (g^r, m_b·y^r)，其中 y 是什么？」（降级子问题） |
| 4 | y 是公钥 g^x？ | 「好。那如果 adversary 能区分 b，它能不能反过来算出一个 g^{xr}？这就是归约到 CDH。」（**L1**：点明归约目标的反诘 —— 给方向，不给步骤） |
| 5 | 那随机预言机用在哪？ | 「H 被建模为随机函数，是为了让 adversary 只能被动问 hash，从而把『区分』逼成『解 CDH』。」（L1） |
| 6 | （写出正确归约草图） | （A5 出题）「换一道变式：改为证明该方案在 *标准* 模型下需要哪个更弱的假设？用同样的归约思路试试。」 |

---

## 6. 学习者模型与视图层（多视图 OLM）

> **定位：次要组件**（论文 Section 4 小节 + Fig. 3，约 1 页）。本文主宣称仍是归因测量装置。
> 设计依据：Bull & Kay (2007) SMILI 框架；Van Labeke et al. (2007) xLM 五层模型（*IJAIED*）。

**层结构**（A2 学情图记忆对外的三个视图）：

| 视图 | xLM 对应层 | 内容 | 数据源 |
|---|---|---|---|
| A 能力画像 | 能力层 | 四维：概念清晰度（提示依赖度）／知识连接度／求助质量（滥用、回避两亚指标）／抗挫坚持力 | 交互日志（口径见 `20_密码学题库与测量.md`） |
| B 科目掌握图 | **领域层**（强制基础层） | 密码学知识点 × 层掌握度（L0–L3，见 `19`） | earned-solve rate 按考点聚合 |
| C 诊断卡 | 错误层 (CAPEs) | 刷题处：错题 → 卡点知识点；自由问答处：**前置不牢 vs 拼接不足** | A2 学情图记忆 + `19` 图谱 |

**红线**：B 的「科目掌握度」**= 主结局 earned-solve rate**，**只作界面展示与效标，不进测量模型**（否则循环论证；见 `12` D11 / D19）。

**三条设计纪律**（实证依据见 `12` D19）：

1. 视图**自选**、默认**概览**、**图形优于文字**（Mabbott & Bull, 2004；Al-Shanfari et al., 2019）
2. 界面**不显示置信区间／不确定性**（Demmans Epp et al., 2026, n=1,653：加入后理解表现下降）
3. 「前置不牢」来自**图谱诊断**、「拼接不足」来自**知识连接度维度** —— **来源不同，内部不得合成单一分数**

**元认知模块（可选）**：可视化「系统判断 vs 学生自信」的不一致以**训练**元认知（Al-Shanfari et al., 2017/2019：后测显著更好、低成就学生受益最大）。
⚠️ 依赖学生自评 → **只作干预功能，不作结果指标**（`12` D11 已砍「元认知校准」为画像维度）。

**交互事件（实现时须打点，供后续统计）**：

```
ViewOpened(student, view_id, ts)            # 学生查看哪个视图（元认知线索）
ConfidenceReported(student, item, conf, ts) # 自评置信（仅干预用，不进模型）
DiagnosisShown(student, card_id, ts)        # 诊断卡展示（干预暴露量）
GuidedToLevel(student, q, level, ts)        # 脚手架实际到达层级（A6 / level_gate 证据）
```

最后一条 `GuidedToLevel` 尤其重要——**它是 D11 那条「87% 级联」在本文场景里的直接检验点**：若我们的 L1 之后也有高比例学生继续要 L2，即复现了 Goldin et al. (2012) 的级联，也就直接印证「约束必须设在低层级」。
