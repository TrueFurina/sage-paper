# 30 · ClawsGO Science 科研提示词包（面向 SAGE 论文线）

> **对象**：`https://app.clawsgo.cn/chat`（ClawsGO Science，云端长程科研 agent）
> **写这套提示词的依据**：该产品自述的能力 + **本项目的既有纪律**（`00/11/25/27/29`）。
> **用法**：母提示词每轮都要挂；任务模板按需取一个；**验收条款不可删**。

---

## 0. 先看它的能力边界（决定怎么用）

**它自述具备**（来源：产品页与聊天页）：

| 能力 | 对本文线的价值 |
|---|---|
| 长程无人值守（数小时、常驻云端、三端同步） | ✅ 适合"检索 → 筛读 → 抽取 → 出表 → 起草"这类长链任务 |
| **真实云浏览器**（与 agent 同会话、可接手） | ✅ **最有用的一条**：期刊官网、DOI、出版商页面都能亲自打开核 |
| 可溯源（环境/命令/文件改动/检索动作逐条留痕、可重放） | ✅ 正合我们"每步有落盘证据"的要求 |
| 代码执行 + LaTeX 工作台（SyncTeX） | ✅ 适合图表复现与投稿稿生成 |
| SSH 接实验室 GPU | ⚪ 本文线暂不需要（我们的是仿真 + 无课堂数据） |
| 出版级图表＝图+代码+环境+版本 | ✅ 正对 `05` §4.2 那三张未画的图 |

**它的边界（必须由提示词补齐）**：
- 它**不知道**我们的口径真值、红线与文件边界 → **每轮都要显式喂**；
- 它**默认倾向"把任务做完"**，而这在我们这类双盲投稿里是风险 → 必须用**停止规则 + 验收条款**约束；
- 它无法判断"某文献是否已被我们自己核过" → 必须要求它**先读 `25`/`26` 再动手**，避免重复劳动或与既有结论冲突。

---

## 1. 🔴 上手前三条安全红线（先读，别跳过）

1. **双盲与未发表保密**：FEDC 是**双盲**（`29` R1/R7）。上传材料前先做：
   - ❌ **不要**把含作者身份/单位/致谢的内容传上去（`27` 已划定边界）；
   - ❌ **不要**把内部决策件（`09` 方向决策、`11` 进度地图、`12` 待决策、`18` 迁移评估、`26` 锐评、`27` 检查清单、`29` 对标）**整包**传上去——它们含"研究策略与自贬性内容"，泄露等于把底牌交出去；
   - ✅ 只传**论文正文与素材**：`01/07/13/14/15/17/21/22/23/24/28` + 需要它核的 `25`（文献档案）。
2. **凭据**：本项目历史上有过**明文密码入库**的事（另一条线的 `pull_new_results.py`）。**任何 key、口令、SSH 私钥都不要贴进对话**。
3. **未发表优先权**：如果该平台会用于训练或对外展示对话，**先把整篇稿子拆成"可公开部分"再传**；拿不准就只传"研究问题 + 待核事实清单"，不传全文。

---

## 2. 母提示词（每轮开头都挂这一段）

```text
【角色】你是科研协作 agent，不是代笔。你的产出要能被审稿人和我逐条复核。

【项目背景】研究题目：When Does Guidance Still Help? Separating Scaffolding Effects from
Answer-Leakage Effects in Graduate Cryptography Instruction — A Simulation Study。
研究形态：**全模拟**（无真实课堂、无真实学生数据）；密码学课程是应用场景，不是数据采集点。

【口径真值（不得改写，与它冲突时以本表为准）】
- 脚手架强度 λ* 的语义 = **守约边界的下沿**（不是"最优强度"，全文不得出现 optimal/optimum）；
- 数字一律用阈值形式：λ* = 0.30，区间 [0.20, 0.45]；earned-solve 峰值 0.5659；
  可归因份额 86%（敏感性 76–88%）；对照臂 +0.0594；完整干预 +0.4380；
  "有教益的泄露"变异体 ≈43%。**旧的连续形式 0.35 / 0.5204 / 85% 一律作废**；
- 泄露量只用 ALR / Telling@N；目标函数里的互信息估计 mi_hat 已证明退化，**不得当作泄露度量**。

【硬约束（违反即判失败）】
1. 不得暗示做过课堂研究、不得出现"我们的学生/被试/课堂"；
2. 不得编造参考文献元数据（卷/期/页/DOI/作者）。**未核到的字段一律写 [待核]，不许猜**；
3. 未取得全文的文献，陈述必须带限定语（如"据其已公开摘要所述"）；
4. 不得写"首创/第一个/无人"类断言；
5. 不得以"别人没有真实学生数据"作为批评点（批评只能落在设计缺陷上：没有把脚手架与少泄题分开）；
6. 目标期刊 FEDC 要求：20–50 页双倍行距（≈≤12,500 词）、APA 6th、摘要 100–120 词、
   关键词 3–5 个、**非英文文献双语著录且标点全用英文**、表与图放在参考文献之后、
   需两个文件（title page + blinded manuscript）、只收 Word。

【工作方式】
- 动手前**先读我给的材料**，并在第一条回复里列出"你读到了什么 / 你判断缺什么"；
- 每个结论都要给出**可核出处**（文件+行、DOI、URL 或命令）与你**实际执行的动作**（命令/检索式/访问的页面）；
- 需要上网时用你的浏览器**真的打开**页面，并把你访问的 URL 贴出来；
- **先交付中间产物让我过目，再往下做**（见每轮的停止规则）。
```

---

## 3. 五个任务模板（按需取一个，接在母提示词后）

### 模板 A · 文献缺口勘查（围绕研究问题找"缺的东西"）

```text
【任务】围绕研究问题做一次可复现的文献缺口勘查。
研究问题：**当守约（不给答案）与脚手架（主动引导）同时作用时，增益中多大比例来自引导、
多大比例只是"少泄了答案"？现有工作是否把这两条通道分开过？**

【输入】我已给你：① 本稿 §1 与 §2 的现有正文（07、13）；② 文献核验档案（25）；
③ 我列的"已核事实"清单（MEMORY-references）。**先读，不要先搜**。

【步骤】
1. 先输出「已知域」：把现有引用按"约束施加在哪一层"分类（提示词 / 输出后处理·输入校验 /
   学习者建模 / 训练目标 / 泄露测量与审计），每条给出它**支撑哪句论断**；
2. 再输出「疑似缺口」：只列**能被检索证实/证伪**的缺口，每个缺口写成可判定的检索命题；
3. 检索：OpenAlex / Crossref / Semantic Scholar / arXiv / ACL Anthology 至少 4 个源，
   每个源给出**完整检索式**与命中数；
4. 反向检索（必做）：对最相关的 3 篇，取作者 id，列出该作者**全部作品**，防漏先例；
5. 去重、两轮筛读（标题+摘要 → 全文可得性），产出 evidence table（列：引用 / 层 / 它做了什么 /
   它没做什么 / 是否支持我们的缺口）；
6. 对每条"它没做什么"，标注**证据等级**：全文级 / 摘要级 / 二手级。

【停止规则】第 1–2 步完成后**停下来等我确认**，不要直接开始大规模检索。
【交付物】① knowns/gaps 表（CSV+MD）；② 检索式与命中数清单；③ 每条缺口的证据等级。
【禁止】不得把"检索没命中"写成"该工作不存在"；只能写"按该检索式未命中"。
```

### 模板 B · 引用核验与元数据补齐（我们最痛的一步）

```text
【任务】把文献表从"核验档案"升级为**可直接投稿的 APA 6th 参考文献表（双语）**。

【输入】`25_AppendixE_Bilingual_References.md`（含 ✅/⚠️/🔴 状态标记与四库反查记录）。

【步骤】
1. 逐条处理 `25` 中标 ⚠️（元数据待补）与 🔴（仅摘要/二手）的条目；
2. **能取全文的必须取全文**（PDF 下载 → 抽文本 → 定位数字 → 打印上下文），
   并给出你实际执行的命令与文件路径；取不到时说明**你试过哪些通道**（出版商页 / DOI 落地页 /
   机构库 / Preprint / 作者主页 / ERIC 全文 / ACL Anthology）；
3. 按 FEDC 要求写成 APA 6th；**中文条目逐字按期刊样例**：
   `作者. (年). 中文题名[English title]. 出版地, 中国: 出版者 [Place, China: Publisher].`
   期刊文章：`… 中文刊名 [English journal name], (期), 页码.`
   **所有标点用英文标点**；
4. **未核到元数据的条目不许进表**——单列一张"未达标清单"给我，不要为了凑齐而填数。

【停止规则】先把"未达标清单 + 你打算怎么补"发我确认，再动手补。
【交付物】① `References_APA.md`（纯文献表，无状态标记、无内部注记）；② 未达标清单；
   ③ 每条全文级核验的**证据行**（页码/表号 + 原文片段）。
【禁止】编造任一字段；把"摘要里没有"写成"该文没有"。
```

### 模板 C · 数字可追溯性审计（防口径漂移）

```text
【任务】把正文里**每一个数字**追回到原始数据/日志/代码，输出可追溯表。
【输入】正文（01/15/17/21/22/23/24/28）+ 数据目录（data/、logs/）+ 脚本（m2_lambda_sweep.py 等）。
【步骤】1. 抽出所有数字（含百分比、区间、置信区间、样本量）；2. 每条给：数值 / 出现位置 /
   来源文件:行 / 产生它的命令或代码路径 / 是否能**重跑复现**；
3. 标注三类问题：① 跨配置混比（拿不同 seed/不同配置的数字比较）；② 旧形式残留
   （0.35 / 0.5204 / 85%）；③ 无出处数字。
【交付物】traceability.csv（列：数值 / 位置 / 来源 / 复现命令 / 判定）+ 问题清单。
【停止规则】只审计，**不要**顺手改正文；改动清单先给我。
【禁止】用"grep 命中的浮点子串"当来源——必须定位到真正的数据字段。
```

### 模板 D · 期刊对标驱动的改稿（把 119 页压到 ≤50 页）

```text
【任务】按目标期刊的官方要求做减法，把本稿压到 20–50 页（双倍行距 12pt，≈≤12,500 词）。
【输入】`29_目标期刊对标_FEDC.md`（含期刊硬要求与三个门槛）+ 全部正文。
【步骤】
1. 先剔"根本不该投"的内容（不是压缩）：内部逐句出处锚定表、内部文件指认（如 `13` 这类）、
   honest-status 节、中文要略块；给出**删除清单 + 删除后词数**；
2. 再按节压缩：§2 文献综述 → ≤2,400 词（六类安置各 2–3 句）、§7 讨论 → ≤2,000 词、
   §1 → ≤1,500 词、附录 → 只留对照推导 + 四套模板 + 3 道示例题；
3. 每砍一处都要**保住**：三处承重论断（归因装置 / 边界语义 / 无泄露独立解出）、
   全部红线限定语、双语著录与参考文献格式；
4. 每轮给我"节 / 原词数 / 现词数 / 砍了什么 / 有无论断受损"的表。
【停止规则】**每完成一节就停**，等我确认再动下一节。
【交付物】压缩后的正文 + 词数复核表 + "被砍内容去向"表（若值得保留则转入附录或内部件）。
【禁止】为达字数而删限定语（如 simulated learners / 未取得全文 / 位置为区间）。
```

### 模板 E · 出版级图表复现（三张待画的图）

```text
【任务】产出三张图的**可复现包**（图 + 生成代码 + 环境 + 版本历史）。
【图 1】约束代价的**阶跃 vs 连续**对照图（横轴 λ，纵轴 delivered fraction / 传递完整性），
  标出边界 λ*=0.30 与区间 [0.20,0.45]，画出连续形式作对照（说明为何不采用）。
【图 2】λ–泄露率 与 λ–earned-solve 双轴曲线（含 0.8524→0.0004 与 0.1279→0.5659→0.2651），
  标出边界与**触底走平**段（0.2642 / 0.2651 区间重叠）。
【图 3】归因分解图：完整干预 +0.4380 vs 仅守约 +0.0594 → 86%，并画出 76–88% 敏感性带。
【要求】数据必须来自仓库内既有结果文件（不许重新编造数据）；每张图给出源数据文件路径与
  生成命令；字体/线宽符合期刊制图要求；**图题注明"simulated"**。
【停止规则】先给数据来源与草图，再出终图。
【交付物】PNG/PDF + 生成脚本 + README（环境与命令）。
```

---

## 4. 验收条款（每轮结束都过一遍，缺一不可）

| # | 检查项 | 判定方式 |
|---|---|---|
| V1 | 每个数字都能追到数据/日志/命令 | 抽查 3 条，要求现场给出复现命令 |
| V2 | 无编造元数据 | 随机抽 3 条参考文献，用 DOI 打开核对 |
| V3 | 未取得全文者带限定语 | 搜 `as described in the published abstract` 类措辞 |
| V4 | 无课堂/学生数据暗示 | 搜 `students in our / classroom study / participants` |
| V5 | 无"首创/无人"断言 | 搜 `first / no existing / none` |
| V6 | 字数与页数达标 | 词数脚本复核（目标 ≤12,500 词） |
| V7 | 双语著录合规（中文条目格式 + 英文标点） | 逐条对期刊样例 |
| V8 | 无作者可识别信息 | 搜单位名、姓名、邮箱 |
| V9 | 过程可溯源 | 要求它列出本轮**实际执行的动作与访问的 URL** |

---

## 5. 反模式（这样用会出事）

| 反模式 | 为什么危险 |
|---|---|
| 「把这篇论文完善一下，缺什么补什么」 | 它会**顺手补引用**——这正是编造元数据的入口。必须改为"缺什么列出来，先别补" |
| 一次让它把整篇重写完 | 我们吃过的教训是**并行/长链改动会静默漂移口径**；必须一节一停 |
| 让它"优化数字表述" | 数字是单一真值，不是措辞 |
| 把 `09/11/12/26/27/29` 一起传上去 | 内部策略与自贬内容外泄 + 双盲风险 |
| 只让它"核一遍引用"，不给核验方法 | 它可能只查摘要 → 会得出"数字无法证实"的**错误结论**（我们在 Goldin 2012 上踩过：87%/88% 只在全文里） |
| 图让它"重新生成数据" | 图必须来自仓库既有结果，禁止另造数据 |

---

## 6. 推荐节奏（本文线当前状态）

1. **今天可派**：模板 A（缺口勘查）+ 模板 B（文献表升级）——两者都在它最强的能力区（浏览器 + 长链检索 + 结构化产出）。
2. **随后**：模板 D（压缩到 ≤12,500 词）——先等 Scope 路径定了再压，否则白改。
3. **并行**：模板 E（三张图）——它自带可复现图表包，正好补上 `05` §4.2 的欠账。
4. **任何一轮结束后**：产出回落到 `E:\Program\MARL\SAGE`，用 `_refcheck.py` 复算关键数字、
   `_charcheck.py`（**必须用 managed python**，见 `MEMORY` §6）查字形、必要时 `md2pdf.py` 渲染，
   并更新 `11`（进度真值源）与 `25`（文献档案）。

---

## 7. 诚实注记

1. 本提示词包基于**产品页的自述能力**写成；**我没有实际登录/调用该服务**，因此对"它的实际表现是否与自述一致"**不作背书**。首次使用请用小任务验证（例如先派模板 C 的一个子集）。
2. 产品页显示的模型名（Claude Fable 5.1 / Opus 5 / GPT 6 Astra）**未经独立核实**，仅为页面原文转述。
3. 若该平台的对话或产物会用于训练/对外展示，则 §1 的保密红线**优先级高于**任何效率考虑。
4. 本文件是**内部工具文档**，不随稿投出。
