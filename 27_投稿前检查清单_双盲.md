# 27 · 投稿前检查清单：双盲（anonymisation）

> 目标期刊 FEDC 采用同行评审。**本文件是投稿前的强制检查表**，逐项打勾后方可提交。
> 建立于 2026-09-22（起因：用户确认"缺口都很重要"，双盲为四项剩余缺口之一）。

---

## A. 先分清：哪些文件是"稿件"，哪些只是"内部记录"

**双盲只约束稿件。内部记录里出现导师/机构/项目名是正常且必要的**（决策文档若不写清"与谁的项目重合"，就失去了决策依据）——
**所以不要为了双盲去改写内部文档，那样只会让文档变模糊**。要做的只有一件事：**别把它们随稿投出**。

| 类别 | 文件 | 处理 |
|---|---|---|
| **稿件（会投出）** | `07` §一（Introduction）、`21` §…、`17`、`13`、`15`、`22`、`23`、`24`、`14`、`01` | 提交前**剥离"★ 中文要略"块与所有 `## … honest-status notes (not for publication)` 节**（见 §C） |
| **素材（按需作附录）** | `02`、`19`、`20` | 若作为附录提交，同样需过本文 §B–D |
| **内部记录（不得投出）** | `00`、`08`、`09`、`10`、`11`、`12`、`18`、`26`、本文件、`FEDC投稿_…研究报告.md`、`.workbuddy/memory/**`、`audit/**` | 🔴 **一律不进投稿包**。其中 `09`/`11`/`12`/`18`/`19`/`FEDC…` 含"导师申报书"等指称，属**有意保留的内部信息** |
| **交付/证据** | `pdf/`、`data/`、`logs/`、`figs/` | `pdf/` 渲染件**含要略块**，不可直接当投稿 PDF |

---

## B. 已完成的清理（2026-09-22）

- ✅ **英文正文本身是干净的**：全篇 `supervisor` 仅出现于描述 Pisan 架构的技术用法；`grant` 仅为 "the request is not granted"；
  `@` 全部是 `Telling@N` 指标名（无邮箱）。
- ✅ 无 `E:\Program…` 类本机路径、无 `鹏城` / `Pengcheng`、无 `Department of` / `Acknowledg` / 基金号。
- ✅ 4 处**语义无损**的措辞收窄（把"导师系统/导师申报书"改为"其自身系统/此前一份立项申报"）：
  `11` L28、`13` L22、`18` L61、`26` L166。

## C. 投稿前必做（尚未做）

1. 🔴 **剥离元信息块**：所有稿件的
   - `> ★ 中文要略（仅供审读；非翻译源，非口径依据 …）`
   - `## … honest-status notes … (not for publication)`
   - 文末的"元信息"引用块（如 `13` §2.7 的"文件复原说明"）
   这些**不是论文内容**，留着会既泄露内部状态、又让审稿人困惑。
   → 建议做法：写一个 `strip_meta.py`（按标题/引用块前缀剥离），**生成 `submission/` 目录**，
   **绝不原地删除**（内部记录仍要留）。
2. **题名页/作者信息**：投稿系统里填，正文文件内不得出现作者、单位、致谢、基金号。
3. **自引模式**：本组若有已发表论文，正文自引须写成第三人称（不写 "our earlier work"）。
4. **文件属性**：导出的 docx/pdf 需清 Office 元数据（作者、最后修改者、公司）；PDF 另查 XMP。
5. **数据/代码可用性声明**：若附仓库链接，**须先确认 commit 元数据不含作者**——
   本项目已把提交身份设为中性值 `SAGE Author <sage-author@localhost>`（见 `MEMORY.md` §0b），此项**已满足**。
6. **图片/图表**：`figs/*.html` 导出为图时，确认无路径水印、无内部代号。
7. **附录 E 双语参考文献**：核对中文条目，确认未夹带内部称呼。

## D. 可复跑的扫描（改完稿随时重跑）

```bash
cd E:/Program/MARL/SAGE
# 1) 稿件文件清单（提交前只对这些做检查）
BODY="01_theoretical_framework_en.md 07_Introduction动机链_原文证据锚定.md 13_Section2_Literature_Review_en.md \
14_Section8_Limitations_en.md 15_Section6_Results_en.md 17_Section5_Method_en.md \
21_Section4_System_Design_en.md 22_Section7_Discussion_en.md 23_Abstract_en.md 24_Section9_Conclusion_en.md"

# 2) 身份/机构/基金/路径痕迹（期望：除 University 外全为 0）
for k in 'E:\\Program' '鹏城' 'Pengcheng' 'Acknowledg' '基金' '@' 'Department of' 'our (lab|institution|university)' '导师' 'supervisor'; do
  printf '%-32s' "$k"; for f in $BODY; do c=$(grep -c -i -E -- "$k" "$f"); [ "$c" != 0 ] && printf '%s(%s) ' "$f" "$c"; done; echo
done

# 3) 要略块是否已剥离（剥离后应为 0）
for f in $BODY; do printf '%-46s %s\n' "$f" "$(grep -c '★ 中文要略' "$f")"; done
```

**判读纪律**：`University` 与 `@` 的命中**不必然**是问题（前者多为被引文献的机构，后者是 `Telling@N`）——
必须**看上下文**再判定；`导师` 在中文里同时指"教学导师"，泛指的不要动。

## E. 与"我们"这个词有关的一条提醒

正文里 `we` / `our` 属正常学术写作（指本文作者），**不构成双盲违规**；
真正会破盲的是：机构名、人名、基金号、可检索到的自引、以及**能指认到具体个人/课题组的指称**（如"导师申报书"）。
