# 08 · M2 实验审计记录（λ−泄露率曲线）

> 用途：记录 2026-09-19 M2 变异检验的全过程、发现的两个真实缺陷、一次**结论被自己推翻**的记录，以及最终可信的曲线形状。
> 纪律：**每个数字都有可复现命令。** 本文件不允许出现「据说」「大概」。
> 结论先行：**Proposition 1 左分支稳固；右分支曾因网格过粗被误判为不存在，修正后成立**——但**A9 仍是条件性假设**，不可淡化。

---

## 一、结论速览

> ### ⚠️ 读这张表之前必读（2026-09-21 口径核查）
>
> **本表的数值属于 A9 的「旧连续/最优点形式」，已随 §八收口被阈值形式取代。**
> 两代数字**不可混用**，凡写进论文一律以**阈值形式**为准：
>
> | 量 | ~~旧形式（本表，已作废）~~ | **阈值形式（现行，论文用）** |
> |---|---|---|
> | 边界位置 λ\* | ~~0.35~~ | **0.30** |
> | earned-solve 峰值 | ~~0.5204~~ | **0.5659** |
> | 下降侧幅度 | ~~0.1394~~ | **0.3008** |
> | 可归因份额 | ~~85%~~ | **86%** |
>
> 旧数值保留在 §四 **只为保留审计轨迹**（记录"当时的判定是什么"），**不得**再被引用为论文数字。
> 阈值形式的来源：`01` §3.2 模拟状态段（40 seeds，θ=0.3578）与本文 §八收口表。

| 项目 | 状态 | 证据 |
|---|---|---|
| 变异检验 | ✅ **9/9 通过**（9 项，含 liveness），基线在 8–60 seeds 稳定 | §三、§五 |
| 死参数 | 🔴 **发现并修复 1 个**（`durable_factor`） | §三.1 |
| 判定逻辑 | 🔴 **发现并修复归因缺陷**（P6 归因对照） | §三.2 |
| 结论翻转 | ⚠️ **网格修正前 REJECTED → 修正后 SUPPORTED** | §四 |
| 最优点 | ~~**λ\* ≈ 0.35**（8/12/20/30/40/60 seeds 一致）~~ → **旧形式，已作废**；现行 **λ\* = 0.30** | §四.4 / §八 |
| 右分支效应量 | ~~**+0.1394**（旧网格 +0.0188，差 7.4 倍）~~ → **旧形式**；阈值形式 **+0.3008** | §四 / §八 |
| 主曲线判定 | ✅ **P1–P6 全通过，SUPPORTED** | §四.4 |
| A9 残余风险 | ⚠️ **仍存在**，见 §六 | §六 |
| PDF 交付链缺陷 | ✅ **第三次同类缺陷已修复 + 变异验证**（陈旧字体子集） | §七 |

**一句话**：这轮不是「让测试变绿」，而是**先发现测试根本没有测试能力，再修复它，然后连续被它推翻了两次自己的判断**
（一次是网格截断右分支，一次是 M3 的期望被粗网格+小样本误导）。

---

## 二、复现命令

```bash
# 变异自检（必须 exit 0）
python m2_lambda_sweep.py --selftest --seeds 12 --problems 12

# 主实验（论文用）
python m2_lambda_sweep.py --seeds 40 --problems 25 --outdir .

# 死参数审计（AST 级，区分"声明"与"读取"）
python _audit_dead_params.py

# 右分支归因裁决
python _adjudicate_m3m4.py
python _refine_grid.py
```

---

## 三、发现的两个真实缺陷

### 3.1 缺陷 A：`durable_factor` 是死参数（声明即文档，代码从不读取）

**发现方式**：变异检验中 M1 把 `durable_factor` 从 `(1.0, 0.72, 0.46)` 改成 `(1.0, 1.0, 1.0)`，**输出逐字节相同**（`peak_gain=0.413@0.2`，theta 也相同）。

**这不是"判据不敏感"，而是"参数根本没进计算"。** 用 AST 审计机械确认：

```
LearnerModel field usage audit
declared fields : 15
read in code    : 14
NEVER read      : 1
*** DEAD PARAMETERS -- documented but never used ***
  - durable_factor         default=(1.0, 0.72, 0.46)
```

**严重性**：该参数在代码里有**长达 6 行的注释说明**，还引用了 Kapur (2008) 的"有效失败"理论，
论证「越显式的帮助 → 越少有效挣扎 → 持久增益越低」。**文档完备、论证充分、却从未参与运算。**
任何"M1 变异体行为不变"的观察都会因此被误读成"该机制不重要"，而真相是**它从未存在**。

**修复**：
1. `simulate()` 中实际应用：`g = scaffold_gain[lvl-1] * durable_factor[lvl-1] * damp`
2. 字段旁加 `!! 2026-09-19 AUDIT FIX !!` 警示块，写明"文档型参数不是机制"
3. 自检新增 **M1 liveness 检查**——不仅比对判定结论，还**比对数值**：
   ```
   ok   M1 liveness: mutating durable_factor CHANGES the reported gain
        (must change; a no-op mutation proves the parameter is dead)
   ```

**副作用（重要）**：修复后曲线变了——最优点从 λ=0.2 移到 λ=0.3，右分支从 +0.0323 变为 +0.0188（旧网格）。
**即：此前所有 M2 数字都是在"少一个机制"的模型上产生的。**

### 3.2 缺陷 B：判定逻辑测错了通道（P6 归因对照）

**发现方式**：M2 变异体把 `scaffold_gain` 置为 `(0,0,0)`——**脚手架被完全删除**——判定依然返回 **SUPPORTED**，峰值 `0.0882 @ λ=0.1`。

**根因**：`lam` 同时进入两个通道：
1. **脚手架通道**：`max_scaffold_level()` 控制升级上限
2. **泄露通道**：`answer_prob()` 中 `z = ... - lam * 12.0`

当脚手架被删除，**泄露通道仍独立产生驼峰**（λ 升高 → 泄露被压制 → earned gain 上升）。
旧判据只检查"内部最优点 + 两端差值"，**无法区分"脚手架有甜点"与"压制答案有甜点"**。

**修复**：新增 **P6 归因对照**——用 `scaffold_gain=(0,0,0)` 跑一条**对照曲线**，
计算"脚手架可归因份额"：

```
可归因增益 = 观测左分支效应 − 对照（无脚手架）左分支效应
脚手架份额 = 可归因增益 / 观测左分支效应     须 ≥ 50%
```

**对照表**（40 seeds，旧网格）：

| 变异体 | 对照左效应 | 观测左效应 | 脚手架份额 | 判定 | 期望 |
|---|---|---|---|---|---|
| BASELINE | +0.0564 | +0.2962 | **81%** | SUPPORTED | SUPPORTED ✅ |
| M2 零脚手架增益 | — | — | ~0% | **REJECTED** | REJECTED ✅ |
| M3 泄露会教学 | +0.1209 | +0.2832 | **57%** | SUPPORTED | SUPPORTED ✅ |
| M4 易解阈值 | +0.0876 | +0.2944 | **70%** | SUPPORTED | SUPPORTED ✅ |

**修复效果**：M2 从"错误通过"变为"正确拒绝"。

### 3.3 关于 P4/P5 的诚实处理

原代码中 P4/P5（置信区间分离）**是绑定条件**，且在 `p1&p2&p3` 全通过时才计算——
意味着**未计算时不会被报告**。已改为**始终计算并打印**，标注 `[recorded, non-binding]`：
小样本下 CI 过宽，绑定会因"样本不够"而拒绝真实分离的曲线。**论文必须同时报告种子数。**

### 3.4 ★ 缺陷 C：`mi_hat` 是退化指标（2026-09-20，最严重）

**现象**：在 `λ ∈ {0, 0.05, 0.10, 0.20, 0.35, 0.50, 0.80}` 全程，`mi_hat` = **3.0000**
（恒为裁剪上限），**即使 λ=0.80 时经验泄露率已降到 0.0003**。

```
lam    mi_hat        alr      earned_gain
 0.00    3.0000    0.8509    0.1292
 0.35    3.0000    0.0550    0.5197
 0.80    3.0000    0.0003    0.3824
```

**根因**（已数值验证）：`p_ans = σ(θ(1−0.5·em) − 12λ)` 是 `em` 的**确定性单调函数**，
且 λ 只改变截距、不改变斜率 → **ρ² 按构造 ≈ 1，与 λ 无关**：

| λ | sd(em) | sd(p_ans) | ρ² | 未裁剪 MI |
|---|---|---|---|---|
| 0.00 | 0.1785 | 3.90e-03 | 0.999999 | 6.9078 |
| 0.35 | 0.1791 | 2.63e-04 | 0.999883 | 4.5249 |
| 0.80 | 0.1768 | 1.21e-06 | 0.999884 | 4.5314 |

`estimate_mi` 计算 `−0.5·ln(1−ρ²)`，于是**它测的是自己的函数形式，不是信息泄露**。

**处置（方案 c，2026-09-20 采纳）**：
- **停止把 `mi_hat` 作为泄露量证据**。论文中的泄露量**只用经验度量**：ALR + Telling@N。
- `01_theoretical_framework_en.md` §3.2 已加入术语注记：`Î(s;a)` 是**训练目标**，**不是测量仪器**。
- `00_定位与口径` 已删除「信息瓶颈给出泄露上界」这一宣称。
- `05_论文大纲` 的 §5.3 与 §8 已写入「不用 `mi_hat` 度量泄露」。

**含义（必须写进 Limitations）**：论文若声称「我们用互信息上界约束泄露」，**必须有独立于 `p_ans` 的 s–a 样本对**；
在 M2 中该独立性**不存在**，必须由 M3 的人工编码 ALR 提供。

**可选修法（未实施）**：(a) 让 `θ` 随难度/知识点变化以制造 `p_ans` 的非退化方差；
(b) 改用**经验互信息**（对 `(em, leaked)` 离散分箱估计）替代高斯 ρ² 公式；(c) 已采纳的诚实方案。

---

## 四、⚠️ 一次结论被自己推翻的记录

### 4.1 事件经过

修正两个缺陷后，用**旧网格**（`0.0 … 0.30, 0.50`）跑 40 seeds，判定 **REJECTED**：

```
P2 left branch : 0.1279 -> 0.5150 (delta +0.3871)  PASS
P3 right branch: 0.5150 -> 0.4962 (delta +0.0188, need >= 0.02)  FAIL
=> NOT SUPPORTED
```

**诱人的错误做法**：把 `min_effect` 从 0.02 降到 0.01 即可通过。
**这是阈值凑数（threshold-shopping），必须拒绝。**

**正确做法**：先问"右分支是真的弱，还是我没量到？"

### 4.2 细化网格后发现：是网格截断了右分支

| λ | earned gain | 95% CI | never-solved | ALR |
|---|---|---|---|---|
| 0.20 | 0.4643 | [0.4624, 0.4663] | 0.320 | 0.2665 |
| 0.25 | 0.5055 | [0.5034, 0.5076] | 0.363 | 0.1628 |
| 0.30 | 0.5150 | [0.5130, 0.5170] | 0.407 | 0.0932 |
| **0.35** | **0.5204** | **[0.5189, 0.5219]** | 0.434 | 0.0545 |
| 0.40 | 0.5168 | [0.5154, 0.5181] | 0.458 | 0.0304 |
| 0.45 | 0.5076 | [0.5058, 0.5094] | 0.478 | 0.0173 |
| 0.50 | 0.4962 | [0.4940, 0.4984] | 0.496 | 0.0094 |
| 0.60 | 0.4648 | [0.4632, 0.4663] | 0.533 | 0.0031 |
| 0.70 | 0.4262 | [0.4241, 0.4283] | 0.573 | 0.0009 |
| 0.80 | 0.3810 | [0.3792, 0.3829] | 0.619 | 0.0003 |

- **真实最优点在 λ ≈ 0.35**，不在 0.30（旧网格因无 0.35 点而误报）
- **右分支 = +0.1394**（0.5204 → 0.3810），是旧网格 +0.0188 的 **7.4 倍**
- **CI 完全分离**：λ=0.40 的 `[0.5154,0.5181]` 与 λ=0.80 的 `[0.3792,0.3829]` 不重叠
- `never_solved` 单调上升 0.32 → 0.62，**锁死机制（lock-out）在校验中可见**

### 4.3 修复：加宽并加密网格（不是降阈值）

```python
DEFAULT_GRID = [0.0, 0.005, 0.01, 0.02, 0.03, 0.05, 0.08, 0.10, 0.15, 0.20,
                0.25, 0.30, 0.35, 0.40, 0.45, 0.50, 0.60, 0.70, 0.80]
```

**判据**：网格必须**在峰值两侧都充分延展**，否则判定无意义。
旧网格的 `0.3 → 0.5` 跳跃恰好**跨过**峰值，且终点 0.5 处下降**才刚刚开始**。

> **方法论要点**：**「判定失败」有两种原因——假设不成立，或测量范围不足。**
> 二者必须用**独立实验**区分，不能靠调参掩盖。本次靠"细化网格"区分，
> 而不是靠"调低阈值"通过。**降阈值会让一个错误的结论看起来正确。**

### 4.4 修正网格后的主结果（40 seeds，论文可用）

| λ | ALR | earned gain | 95% CI | leak-solves | never-solved |
|---|---|---|---|---|---|
| 0.000 | 0.8524 | 0.1279 | [0.126,0.129] | 0.6701 | 0.2020 |
| 0.050 | 0.7309 | 0.2151 | [0.213,0.217] | 0.5801 | 0.2047 |
| 0.100 | 0.5772 | 0.3113 | [0.309,0.314] | 0.4620 | 0.2266 |
| 0.150 | 0.4077 | 0.3976 | [0.395,0.400] | 0.3296 | 0.2728 |
| 0.200 | 0.2665 | 0.4643 | [0.462,0.466] | 0.2159 | 0.3198 |
| 0.250 | 0.1628 | 0.5055 | [0.503,0.508] | 0.1317 | 0.3628 |
| 0.300 | 0.0932 | 0.5150 | [0.513,0.517] | 0.0783 | 0.4067 |
| **0.350** | **0.0545** | **0.5204** | **[0.519,0.522]** | **0.0457** | **0.4338** |
| 0.400 | 0.0304 | 0.5168 | [0.515,0.518] | 0.0253 | 0.4579 |
| 0.500 | 0.0094 | 0.4962 | [0.494,0.498] | 0.0080 | 0.4958 |
| 0.600 | 0.0031 | 0.4648 | [0.463,0.466] | 0.0025 | 0.5327 |
| 0.700 | 0.0009 | 0.4262 | [0.424,0.428] | 0.0008 | 0.5730 |
| 0.800 | 0.0003 | 0.3810 | [0.379,0.383] | 0.0002 | 0.6187 |

**判定（40 seeds，θ=0.3578）**：

```
P1 interior optimum at lambda=0.35 (index 12 of 0..18)              PASS
P2 left branch : 0.1279 -> 0.5204 (delta +0.3925, need >= 0.02)     PASS
P3 right branch: 0.5204 -> 0.3810 (delta +0.1394, need >= 0.02)     PASS
P6 ATTRIBUTION : control left_effect=+0.0594, observed=+0.3925,
                 scaffolding share=85% (need >= 50%)                 PASS
P4 CI separation vs lambda=0  [recorded, non-binding]               PASS
P5 CI separation vs lambda=max [recorded, non-binding]              PASS
=> SUPPORTED
```

**标定门 G1–G5 全部 ok**。`never_solved` 0.202 → 0.619 与 `ALR` 0.852 → 0.000 同步演变，
**两条通道（压制泄露 / 锁死升级）在数据里都可见**。

~~**论文可用的三句话**~~ → **旧形式，2026-09-21 作废，仅为审计轨迹，不得进论文**
（阈值形式的现行表述见 `01` §3.2 模拟状态段与本文 §八收口）：
1. ~~存在内部最优 λ* ≈ **0.35**（earned gain 峰值 0.5204）。~~ → 现行为**守约边界 λ\* = 0.30**（峰值 0.5659），
   且语义已由「内部最优强度」改为「**边界下沿**」——不只是数字变了，**说法也变了**。
2. ~~右分支成立：λ=0.8 时增益降至 0.3810（**−0.1394**）~~ → 阈值形式：λ=0.8 时 earned-solve 降至 **0.2651**，
   下降侧 **+0.3008**（CI 与峰值不重叠）。结论不变：**过度约束的代价是学习者被拒之门外**。
3. ~~**85%** 的改善可归因于脚手架~~ → 阈值形式 **86%**，而非单纯的泄露压制（P6 对照）。

---

## 五、变异检验最终结果（9/9 → ★ 现行口径 13/13）

> **★ 2026-09-21 复跑更新（口径同步）**：本节标题与代码块中的 **9/9** 是**历史版本**的记录
> （当时的 9 个变异体）。现行脚本已扩充至 **13 项**（新增 M8 阈值机制 / M9 边界左移 +
> **3 个 liveness 检查**：`durable_factor`、`collapse_threshold`、`collapse_sharpness`）。
> **2026-09-21 实测：`--seeds 16 --problems 16` → 13/13 通过，exit 0。**
> 论文与一切对外表述一律用 **13/13**；9/9 仅作本文件的历史留痕。
>
> **⚠️ 同次复跑发现一条有约束力的副作用**：在 `--seeds 8 --problems 8` 规模下 **M3 FAIL**
> （leak_durable=0.50 时应 REJECTED，实测 SUPPORTED，脚本据此自行拒绝背书 Proposition 1）；
> 16 seeds 起恢复正确。**判据不是样本量无关的** —— 样本量不足时它会把泄漏通道产生的效应
> 错记到脚手架账上，即**恰好犯了该装置本应防止的那个错误**。已写入 `15` §6.1.4，并登记为
> `12` D12 的硬约束（M3 功效必须远高于此阈值）。

**验证命令**：`python m2_lambda_sweep.py --selftest --seeds 20 --problems 20` → **exit 0**

```
ok   BASELINE (unmutated)                    peak=0.484@0.35  SUPPORTED  expect=SUPPORTED
ok   M1 no durable attenuation (all 1.0)     peak=0.516@0.25  SUPPORTED  expect=SUPPORTED
ok   M2 scaffolds give zero mastery gain     peak=0.100@0.1   REJECTED   expect=REJECTED
ok   M3 answer leaks DO teach (0.25)         peak=0.521@0.25  REJECTED   expect=REJECTED
ok   M4 easy solve threshold (0.20)          peak=0.530@0.25  SUPPORTED  expect=SUPPORTED
ok   M5 no delivery damping (0.0)            peak=0.591@0.7   REJECTED   expect=REJECTED
ok   M6 no gate + no damping                 peak=0.821@0.8   REJECTED   expect=REJECTED
ok   M7 no damping + no frustration          peak=0.721@0.8   REJECTED   expect=REJECTED

ok   M1 liveness: mutating durable_factor CHANGES the reported gain

[SELFTEST] 9/9 mutation checks passed
```

**判据含义**：4 个"必须被拒绝"的变异体（M2/M3/M5/M6/M7）全部被拒绝，
说明**诊断对该机制的依赖是真实的**；M4 保持 SUPPORTED 且理由可查（§5.3）。
基线在所有种子数下稳定给出 λ*=0.35（**旧形式**；阈值形式为 **0.30**，见 §一顶部口径警告与 §八）。

### 5.1 M3 / M4 的期望被修正（诚实记录）

**初版我预期 M3、M4 应被 REJECTED，旧网格 12 seeds 下实测均 SUPPORTED。当时裁决——"是我的预期错了"。**

| 变异体 | 我的初始预期 | 旧网格 12seeds | 新网格 8–60seeds | 最终裁决 |
|---|---|---|---|---|
| M3 `leak_durable=0.25` | REJECTED | SUPPORTED | **REJECTED（稳定）** | **预期原是对的** |
| M4 `solve_threshold=0.20` | REJECTED | SUPPORTED | SUPPORTED | 预期错误 |

**我犯的错**：在**旧网格、12 seeds** 下看到 M3=SUPPORTED，就写下"预期错误、代码正确"，
并把它当成一次诚实的自我纠正。**实际上那是被粗网格与小样本共同误导的第二重误判。**

### 5.2 ★ M3 的种子阶梯裁决（决定性证据）

为区分"统计噪声"与"实质性边界"，跑种子阶梯（8→60）：

```
M3 (leak_durable=0.25)
 seeds   theta    peak   @lam     left    right  attr%    P2    P3   verdict
     8    1.75  0.5430   0.25  +0.3961  +0.1615    40%  PASS  PASS  REJECTED
    12    1.75  0.5453   0.25  +0.3997  +0.1632    40%  PASS  PASS  REJECTED
    20    1.75  0.5458   0.25  +0.3993  +0.1643    40%  PASS  PASS  REJECTED
    30    1.75  0.5461   0.25  +0.3990  +0.1647    40%  PASS  PASS  REJECTED
    40    1.75  0.5452   0.25  +0.3977  +0.1638    40%  PASS  PASS  REJECTED
    60    1.75  0.5447   0.25  +0.3980  +0.1627    40%  PASS  PASS  REJECTED

BASELINE
     8  0.5199   0.35  +0.3916  +0.1354   SUPPORTED
    40  0.5204   0.35  +0.3927  +0.1394   SUPPORTED
    60  0.5191   0.35  +0.3912  +0.1378   SUPPORTED
```

**结论（三条，均已排除其他解释）**：

1. **M3 不是噪声翻转**：`right` 稳定在 +0.16，`attr%` 稳定在 **40%**，θ 稳定在 1.75，
   **8 到 60 seeds 完全一致**。噪声假说（H1）排除。
2. **失败在 P6 归因，不在 P2/P3**：`left`/`right` **都 PASS**，是**归因份额 40% < 50%** 导致 REJECTED。
3. **翻转来自网格细化，不来自种子数**：旧粗网格上 M3 的归因份额 ≥50%；
   细化网格解析出了中间 λ 区间，**泄露压制通道在其中的相对贡献更大**，
   于是脚手架可归因份额降到 40%。

**实质性含义（这才是 M3 真正有价值的地方）**：
当 `leak_durable` 从 0.012 提高到 0.25（**泄露的答案真的能教会人**）时，
甜点的改善中**只有 40% 来自脚手架**，另外 60% 来自单纯压制泄露。
即：**当答案本身有教学价值时，「脚手架」与「少泄露」就难以分离** ——
这正是 SAGE 主张「必须把泄露量做成可观测量」的**最强内部证据**：
不测量，就无法分辨你改善的到底是引导质量，还是泄露量。

**处置**：M3 期望**改回 REJECTED**（初始预期是对的），并在代码注释中记录这段曲折，
因为**"我改过一次又改回来"本身就是该变异体最重要的注脚**。
**M4 维持 SUPPORTED**（期望错误，理由见下）。

### 5.3 M4 为何保持 SUPPORTED（唯一确认的"期望错误"）

- M4 降低的是**解题门槛**（`solve_threshold` 0.62 → 0.20），
  而 `level_gate`（A7）是**独立约束**，仍然生效。
- 实测右分支 +0.0358，归因份额 **70%** — 机制完好，**SUPPORTED 是正确的**。

> **教训（三层，逐层加深）**：
> 1. 写变异检验时，**期望必须来自机制分析，不能来自"我觉得应该"**。
> 2. **但"代码是对的"本身也需要证据** —— 必须能指出"哪个受约束的判据在起作用"。
> 3. **小样本 + 粗网格会同时制造假阳性与假阴性。**
>    本轮 M3 在旧网格 12 seeds 显示 SUPPORTED，我据此写下"预期错误"，
>    后被种子阶梯证明**原预期才是对的**。
>    **"我承认自己错了"如果不基于足够证据，也只是另一种不严谨。**

---

### 5.4 ★ M3 的重标定：一次由「改功能形式」引发的变异体失配（2026-09-20）

**现象**：A9 由连续线性改为阈值形式后，`--selftest` 报 **M3 FAIL**——
旧的 `leak_durable=0.25` 变异体从 REJECTED 变成了 **SUPPORTED**。

**这不是噪声，是功能形式改变的必然结果。** 旧形式下脚手架路线随 λ 被**渐进削弱**，
所以观测到的上升幅度被压低，归因份额容易掉到 50% 以下；阈值形式下脚手架在 λ_c 之前
**满强度传递**，其相对贡献更大 → 同样的泄露可教性下，归因份额更高。

**处置方式（关键，不是重新挑一个能过的值）**：对 `leak_durable` 做**扫描**，定位翻转点。

> ### ⚠️ 口径警告（2026-09-22 新增）—— 本扫描的数字不得直接写进论文
>
> 本表的运行配置**与 §6.1 主结果（40 seeds / 240 learners / 25 problems）不同**，
> 因此其基线份额 **84%** 与主结果的 **86%** **不可并列**，也不可互相印证。
> 这正是 2026-09-22 锐评发现的口径冲突：`01` §3.2 caveat (iii) 引用本表的 84%，
> 而 `12` / `15` / MEMORY 的"单一真值"是 86%，两者同为「阈值形式」却相差 2 个百分点。
>
> **处置**：已按主结果同配置重跑该扫描（`_ld_sensitivity.py --seeds 40 --problems 25 --learners 240`），
> 结果见 `data/leak_durable_sensitivity_*.json` 与论文 `15` §6.3.1。
> **论文一律引用 §6.3.1 的 40-seed 值**；本表只保留为审计轨迹（"当时的判定是什么"），
> 其**定性**结论（份额随 `leak_durable` 单调下降并跨过 50% 线）成立且已被新扫描复核。
>
> 🔴 **更严重的是：翻转点数字本身也是错的，不止基线份额。**
> 本表记「翻转点 ≈ 0.45」；40-seed 主配置实测：**ld=0.20 → 52%（SUPPORTED）、ld=0.22 → 48%（REJECTED）**
> → **翻转点在 (0.20, 0.22)，即 ≈0.21，不是 0.45**（误差 >2 倍）。
> 该错误已扩散到三处正文：`01` §3.2 caveat (iii)、`22` §7.1、本表下方正文。
> **三处均已按主配置值修正（2026-09-22）。**
> 修正方向对论文**不利**（翻转更早 = 86% 的稳健区间更窄），故不是挑好看的数字。

| leak_durable | 0.012（基线） | 0.10 | 0.15 | 0.20 | 0.25 | 0.30 | 0.35 | 0.40 | **0.50** | 0.70 |
|---|---|---|---|---|---|---|---|---|---|---|
| **归因份额** | **84%** | 76% | 71% | 68% | **64%** | 60% | 55% | 52% | **43%** | 40% |
| 判定 | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | SUPPORTED | **REJECTED** | REJECTED |

**翻转点 ≈ 0.45**，份额从 84% **单调**降到 40% —— 说明归因对照**活着且响应平滑**，
50% 这条线落在一条真实的连续曲线中间，而不是一个碰巧通过的数字。

**为什么这不是「threshold-shopping」（把阈值往下调以让结论通过）**：
- threshold-shopping 是**改判据**（调低 `min_effect` 或 `attribution_ratio`）以迎合结论；
- 这里**判据一个字没改**（仍是 50%），改的是**变异体的取值**，并且是**先测出翻转点、再放到翻转点之外**；
- 而且**两侧都被断言**（见下），所以 50% 是被**测出来的边界**，不是我挑出来的。

**变异体最终设置**（`_check_threshold_mutants.py` 与 `--selftest` 同步）：

> ⚠️ **下面两行的实测值是变异检查自身配置下的读数**（`_check_threshold_mutants.py` 默认 `--seeds 10 --problems 12 --learners 120`；
> 2026-09-22 实跑复核 **7/7 通过**）。**不可**与主结果（40 / 240 / 25）的份额并列引用。

- `leak_durable = 0.50`（该配置翻转点之外）→ 必须 **REJECTED**（实测 attr **43%**）
- `leak_durable = 0.35`（该配置翻转点之内）→ 必须 **SUPPORTED**（实测 attr **55%**）

> 🔴 **配置依赖提醒（2026-09-22 审计补）**：40 seeds 主配置实测翻转点在 **(0.20, 0.22)**，
> 因此"0.35 落在翻转点之内"**只对该变异检查的小配置成立**；在主配置下 0.35 已在翻转点之外
> （ld=0.40 → 32% REJECTED）。
> 论文若引用"两侧断言"，必须带配置限定，或改用**主配置**的两侧值：
> **0.20 → 52% SUPPORTED**（内）与 **0.50 → 28% REJECTED**（外），二者同出 `15` §6.3.1 的 40-seed 扫描。
> 另：**43% / 55% 均属小配置读数**，不得写成主配置份额（主配置 ld=0.50 → **28%**）。

> **两侧断言才使这条 50% 线有意义。** 只测一侧，任何阈值都能"通过"。

**⚠️ 本节上方 §5.2 的 M3 种子阶梯（attr% 恒为 40%）是在旧连续线性形式下测得的，已被本节取代。**
其中「翻转来自网格细化而非种子数」的**方法论结论仍然成立**（它比较的是网格与种子的相对贡献，
与功能形式无关）；但 **40% 这个具体数值不可再引用**。

**净影响（对论文有利）**：阈值形式下，基线的**归因份额从 85%→84% 基本持平**，
而**同一泄露可教性下的归因更抗稀释**（0.25 处由 40% 升到 64%）。
即：**改功能形式没有削弱归因宣称，反而使它更稳。**



## 六、⚠️ A9 残余风险（必须写入 Limitations）

> **★ 2026-09-20 更新：A9 已由「连续线性」改为「阈值/边界」形式（D6）。** 本节已按新形式重写。

**A9 仍是条件性假设，本轮的工作没有改变这一点。**

- **功能形式已改为有文献依据的形式。** 旧式 `damp = 1 − D·min(1,λ)`（连续线性）与实测的**阶跃崩塌**机制不符；
  新式 `damp = 1 − D·sigmoid((λ−λ_c)/s)` 采纳 arXiv 2604.13006 的 **collapse floor** 证据
  （"a discrete strategy switch rather than continuous degradation"）。
  **注意：文献给的是形态（阶跃 + 地板），不是量级。**
- **三个常数都没有实证来源**，均为**规定值**：`delivery_damping=0.75`（崩塌深度）、
  `collapse_threshold=0.40`（λ_c）、`collapse_sharpness=0.04`。
- **变异体证明机制是活的**：M5（深度=0）、M8（λ_c 移出网格）、M9（λ_c 在最左）**均被正确 REJECTED**；
  而 SANITY（λ_c=0.55，仍可达）**保持 SUPPORTED** → 机制**敏感但不脆**。
- **★ 命题语义已随之改变（论文必须同步）**：λ\* 不再表示「最优引导强度」，
  而表示**守约边界下沿**（= 最后一个还能教的档位）。**禁止再写「内部最优 / interior optimum / sweet spot」。**
- **论文必须**：
  1. Proposition 1 表述为**「守约边界位于 λ\*」**，并说明这是**在 A9 成立的前提下**成立
  2. 报告 λ\* 的**位置**对三个 A9 常数的敏感性（报**区间**），并说明其**存在性**与**归因份额**不敏感
  3. 明确写：`durable_factor`、`delivery_damping`、`collapse_threshold`、`collapse_sharpness`、
     `frustration_rate` 为**规定值（stipulated）**，非实测值

---

## 七、附：PDF 交付链的第三次同类缺陷（2026-09-19）

**不是 M2 实验的问题，但记录在此，因为它是同一类错误思维方式的第三次发作。**

**症状**：本文档 `08_M2实验审计记录.pdf` 渲染出 **4467 KB**，其余 10 份 PDF 全在 745–1215 KB。
覆盖率检查报 `coverage OK`（0 未覆盖字符），**完全没拦住**。

**根因（顺序坑，非断言写错）**：

```
1. 编辑 08_M2实验审计记录.md   → 引入新字形（旧 峰 粗 噪 刚 至 曾 械 挣扎 久 旁 副 驼 跃 犯 辨 槛 阳 阴 谨 脆 消 漂）
2. python md2pdf.py            → 拿【上一次】的 _sage_subset.ttf 渲染 →
                                  PyMuPDF 静默内嵌 3.4 MB Droid Sans Fallback
                                  （无报错、无警告，唯一症状是体积大 4 倍）
3. python _fontsub.py          → 重建子集（已太晚，PDF 已脏）
```

**为什么断言查不出**：覆盖率检查的逻辑是「读 `.md` 字符集 → **现场构建**子集 → 断言子集覆盖该字符集」。
子集是从同一份字符集现场造的，所以**永远自洽** —— 它在验"新配的钥匙开新配的锁"，
**原理上不可能发现"磁盘上那把旧钥匙开不了新锁"**。

**被证伪的 7 个假设**（记录以免重走）：emoji 回退 / 子集缺字形（**24 字全在 cmap 里**）/
cmap format-4 溢出（`numGlyphs=1314`，无越界）/ OS/2 缺 CJK 位（**bit48 已置位**）/
字符本身有问题（**单字渲染 24/24 正常**）/ 相邻字符粘连（复刻真实短语全部正常）/
全文进程内渲染复现（**0 个回退 span**）。

**决定性判据**：单独重跑 `md2pdf.py` 渲染本文档 → **体积自动回落 993 KB**。
→ 脏的不是文档、不是字体、不是渲染器，而是**「渲染时用的那份子集」**。

**复现与验证**（换用只含他文档字符集的陈旧子集）：

| 子集 | 体积 | 回退 span | 内嵌字体 |
|---|---|---|---|
| 陈旧 | **5008 KB** | **723** | Droid Sans Fallback / Noto Sans Math / Noto Sans Symbols2 / Noto Serif |
| 正确 | **993 KB** | **0** | Microsoft YaHei Regular |

**修复（三层，前两层是自动防御，第三层是提交门禁）**：

1. **渲染后守卫** `md2pdf.unembedded_fallback_fonts()` —— 读**最终 PDF** 的内嵌字体，
   命中回退字体就打印修复指令并置非零退出码。
   > 注意：**不能用 CSS 别名 `sagecjk` 判断** —— 子集化保留 name 表，
   > 子集在 PDF 里的真实名是 `Microsoft YaHei Regular`。只能按**外来字体名**判。
2. **渲染前检查** `md2pdf.subset_covers()` —— 发现子集不覆盖源码就自动重建。
   > 坑：`'\n'` 在正常字体里**没有字形**，若不排除控制字符，**每份文档都会被误判未覆盖**。
3. **提交前门禁** `_pdf_hygiene.py` —— 遍历最终 PDF 查回退字体 + 字体负载占比
   （`payload/size > 92%` 且 `payload > 2 MB` 判膨胀），fail-closed。

**变异验证**（`_munge_subset_guard.py` / `_munge_pdf_hygiene.py`，全部 PASS）：

| 检查项 | 结果 |
|---|---|
| 前置检查能识别陈旧子集并重建 | ✅ `preflight_rebuilt=True` |
| 后置守卫能检出脏 PDF（绕过前置直调渲染） | ✅ 检出 4 种回退字体、5008 KB |
| 正确子集不误报 | ✅ `fresh_no_rebuild=True` |
| 卫生门禁能 FAIL（构造带回退字体的 PDF） | ✅ 检出 641 回退 span |

> ⚠️ **第一次变异检验给出的是假 FAIL**：前置自动重建先把子集修好了，
> 后置守卫根本没被触发到。**两个机制必须各自独立验证** ——
> 否则会误以为"守卫没用"，或更糟：误以为"守卫有用"其实它从没跑过。

**最终状态**：`md2pdf.py` 11/11 渲染、`_pdfverify.py` 11/11 内容校验、
`_pdf_hygiene.py` 11/11 卫生检查 —— **三道全绿**。

### 7.1 ★ 写完本节后，守卫当场又抓了一次（自证有效）

本节内容本身引入了 **22 个新字形**（`【】八匙卫各坑嵌晚染永洽渲溢症粘糟胀脏膨钥陈`），
于是**又一次触发同一个失败模式** —— 但这次**第一道闸门就亮了**：

```
$ python _pdf_hygiene.py          → 11/11 PASS   （PDF 是干净的，渲染在编辑之前）
$ subset_covers(all .md)          → False        （前置检查发现子集已过期）
$ python md2pdf.py 08_M2...       → [subset] 自动重建 → 1064 KB，0 回退字体
$ subset_covers(all .md)          → True
```

**文件时间戳是决定性证据**：

| 文件 | 时间 | 说明 |
|---|---|---|
| `_sage_subset.ttf` | 21:01:47 | 旧子集 |
| `pdf/08_M2实验审计记录.pdf` | 21:01:47 | 用旧子集渲染（当次渲染时源文件尚未编辑 → **干净**） |
| `08_M2实验审计记录.md` | **21:02:20** | **编辑发生在渲染之后** → 引入 22 个新字形 |

**这在客观上形成了一次无人为设计的端到端验证**：
失败条件真实出现 → 前置检查**主动识别** → 自动重建 → 重新渲染干净 → 后置检查确认。

> **注意此处没有发生体积膨胀**：因为"编辑源文件"与"用旧子集渲染"**没有连在一起发生**。
> §七 描述的真实事故之所以膨胀 4 倍，是因为那一次**编辑完立刻渲染、中间没重建子集**。
> → **失败的必要条件不是"子集旧"，而是"子集旧 + 紧接着渲染"。**
> 这解释了为什么这个 bug **难以复现、难以察觉**：它依赖时序，不依赖任何静态属性。

---

## 八、待办

> **★ 2026-09-20 收口更新**：A9 改阈值形式后，下列多项已完结或无意义（如「λ≈0.35 / right +0.1394」属旧形式数值）。

- [x] ~~A9 敏感性分析（`delivery_damping ∈ {0.5,0.75,1.0}`）~~ → **已扩为三常数全扫**（`collapse_threshold` / `delivery_damping` / `collapse_sharpness`），
  全部 SUPPORTED（15/15 非退化设定），见 `09` §七.1–七.2
- [x] ~~`durable_factor` 敏感性~~ → **已完成**（`_df_sensitivity.py`）：参数**承重**（c=0 vs c=1：gain 0.4957→0.5258，λ\* 0.30→0.25），
  但 λ\* 对其仅敏 0.05；**另有 θ 补偿耦合须声明**，见 `09` §七.3
- [x] ~~把 λ≈0.35 与 right +0.1394 同步进 `01`~~ → **已替换为阈值形式 40-seed 数字**（λ\*=0.30、gain 0.5659、right +0.3008、归因 86%），并附 collapse floor 的 CI 表
- [x] ~~`_audit_dead_params.py` 不在磁盘上，需重建~~ → **已重建并跑通**：**17/17 字段全 live**（已修正该脚本自身的「单观测量误报」缺陷）
- [x] ~~变异检验~~ → **13/13，EXIT=0**；另新增阈值专属变异体 M8/M9 与泛化 liveness（3 字段）
- [x] ~~PDF 体积异常（4467 KB）~~ → 已定位为陈旧子集，三层修复 + 变异验证完成，见 §七
- [ ] **提交前用 `--seeds 100` 复核 λ\* 位置**（当前主结果 40 seeds；敏感性 20 seeds）
- [ ] **`05` §4.2 那张「传递完整性 vs λ」图必须同时画出 `max_scaffold_level` 的层级上限台阶**
      —— 基线 λ\*=0.30 与 cap 阈值数值相同（虽已证 λ\* 由崩塌边界而非 cap 决定，但审稿人看数字无从分辨）
- [ ] Limitations 须写：`mi_hat` 估计器退化已停用；三个 A9 常数 + `durable_factor` 均为**规定值**；θ 与 A9 存在**补偿耦合**
